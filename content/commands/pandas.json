{
  "library": "pandas",
  "title": "Pandas",
  "officialDocs": "https://pandas.pydata.org/docs/",
  "subtopics": [
    "I/O",
    "Selection",
    "Transformation",
    "Join",
    "Aggregation",
    "Missing Data",
    "Time Series",
    "EDA"
  ],
  "commandGroups": [
    {
      "name": "Load and Inspect",
      "docs": [
        {
          "label": "Pandas IO Tools",
          "url": "https://pandas.pydata.org/docs/user_guide/io.html"
        }
      ],
      "commandIds": [
        "pandas-read-csv",
        "pandas-head",
        "pandas-sort-values"
      ]
    },
    {
      "name": "Selection and Transformation",
      "docs": [
        {
          "label": "Indexing and Selecting",
          "url": "https://pandas.pydata.org/docs/user_guide/indexing.html"
        }
      ],
      "commandIds": [
        "pandas-loc",
        "pandas-query",
        "pandas-assign",
        "pandas-fillna",
        "pandas-to-datetime",
        "pandas-datetime-filter"
      ]
    },
    {
      "name": "Joins and Aggregation",
      "docs": [
        {
          "label": "Merge, Join, Concat",
          "url": "https://pandas.pydata.org/docs/user_guide/merging.html"
        },
        {
          "label": "Group By",
          "url": "https://pandas.pydata.org/docs/user_guide/groupby.html"
        }
      ],
      "commandIds": [
        "pandas-merge",
        "pandas-groupby-agg",
        "pandas-pivot-table",
        "pandas-resample",
        "pandas-groupby-basic",
        "pandas-groupby-named-agg",
        "pandas-groupby-share-total"
      ]
    },
    {
      "name": "Extended APIs and Step-by-Step Patterns",
      "docs": [
        {
          "label": "Pandas official docs",
          "url": "https://pandas.pydata.org/docs/"
        }
      ],
      "commandIds": [
        "pandas-read-excel",
        "pandas-info",
        "pandas-describe",
        "pandas-iloc",
        "pandas-drop",
        "pandas-rename",
        "pandas-astype",
        "pandas-value-counts",
        "pandas-groupby-transform",
        "pandas-melt",
        "pandas-concat",
        "pandas-join",
        "pandas-drop-duplicates",
        "pandas-isna",
        "pandas-dropna",
        "pandas-replace",
        "pandas-cut",
        "pandas-qcut",
        "pandas-rolling",
        "pandas-shift",
        "pandas-dt-year",
        "pandas-dt-components",
        "pandas-corr",
        "pandas-nunique"
      ]
    },
    {
      "name": "EDA and Datetime Playbook",
      "docs": [
        {
          "label": "Pandas datetime guide",
          "url": "https://pandas.pydata.org/docs/user_guide/timeseries.html"
        },
        {
          "label": "Pandas groupby guide",
          "url": "https://pandas.pydata.org/docs/user_guide/groupby.html"
        }
      ],
      "commandIds": [
        "pandas-to-datetime",
        "pandas-dt-year",
        "pandas-dt-components",
        "pandas-datetime-filter",
        "pandas-groupby-basic",
        "pandas-groupby-agg",
        "pandas-groupby-named-agg",
        "pandas-groupby-transform",
        "pandas-groupby-share-total",
        "pandas-corr",
        "pandas-nunique"
      ]
    }
  ],
  "cheatSheet": [
    "pandas-read-csv",
    "pandas-head",
    "pandas-loc",
    "pandas-assign",
    "pandas-query",
    "pandas-merge",
    "pandas-groupby-agg",
    "pandas-pivot-table",
    "pandas-sort-values",
    "pandas-fillna",
    "pandas-to-datetime",
    "pandas-resample",
    "pandas-read-excel",
    "pandas-info",
    "pandas-describe",
    "pandas-iloc",
    "pandas-drop",
    "pandas-rename",
    "pandas-astype",
    "pandas-value-counts",
    "pandas-groupby-transform",
    "pandas-melt",
    "pandas-concat",
    "pandas-join",
    "pandas-drop-duplicates",
    "pandas-isna",
    "pandas-dropna",
    "pandas-replace",
    "pandas-cut",
    "pandas-qcut",
    "pandas-rolling",
    "pandas-shift",
    "pandas-dt-year",
    "pandas-dt-components",
    "pandas-datetime-filter",
    "pandas-groupby-basic",
    "pandas-groupby-named-agg",
    "pandas-groupby-share-total",
    "pandas-corr",
    "pandas-nunique"
  ],
  "coverageOutline": [
    {
      "topic": "Indexing and Selection",
      "docUrl": "https://pandas.pydata.org/docs/user_guide/indexing.html",
      "status": "seeded",
      "highUsageApis": [
        "loc",
        "iloc",
        "query",
        "filter"
      ]
    },
    {
      "topic": "Window Functions",
      "docUrl": "https://pandas.pydata.org/docs/user_guide/window.html",
      "status": "planned",
      "highUsageApis": [
        "rolling",
        "expanding",
        "ewm"
      ]
    },
    {
      "topic": "Missing Data",
      "docUrl": "https://pandas.pydata.org/docs/user_guide/missing_data.html",
      "status": "seeded",
      "highUsageApis": [
        "isna",
        "fillna",
        "dropna",
        "interpolate"
      ]
    },
    {
      "topic": "Reshaping",
      "docUrl": "https://pandas.pydata.org/docs/user_guide/reshaping.html",
      "status": "seeded",
      "highUsageApis": [
        "pivot_table",
        "melt",
        "stack",
        "unstack"
      ]
    },
    {
      "topic": "Datetime Feature Engineering",
      "docUrl": "https://pandas.pydata.org/docs/user_guide/timeseries.html",
      "status": "seeded",
      "highUsageApis": [
        "to_datetime",
        "dt.year",
        "dt.month",
        "dt.day_name",
        "resample"
      ]
    },
    {
      "topic": "GroupBy Analytics Patterns",
      "docUrl": "https://pandas.pydata.org/docs/user_guide/groupby.html",
      "status": "seeded",
      "highUsageApis": [
        "groupby",
        "agg",
        "transform",
        "nunique"
      ]
    },
    {
      "topic": "EDA Profiling",
      "docUrl": "https://pandas.pydata.org/docs/user_guide/basics.html",
      "status": "seeded",
      "highUsageApis": [
        "info",
        "describe",
        "nunique",
        "corr"
      ]
    }
  ],
  "commands": [
    {
      "id": "pandas-read-csv",
      "slug": "pandas-read-csv",
      "name": "pandas.read_csv",
      "library": "pandas",
      "subtopic": "I/O",
      "tags": [
        "io",
        "csv",
        "loading"
      ],
      "level": "core",
      "summary": "Read CSV files into a DataFrame.",
      "syntax": "pd.read_csv(filepath_or_buffer, sep=',', usecols=None, dtype=None, parse_dates=None)",
      "description": "Read CSV files into a DataFrame. Use it for ingesting exports and serializing clean outputs and cleaning and reshaping transaction/event tables. Key parameters: filepath_or_buffer, usecols, dtype.",
      "docs": [
        {
          "label": "pandas.read_csv",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.read_csv.html"
        }
      ],
      "parameters": [
        {
          "name": "filepath_or_buffer",
          "type": "str | path-like | file-like",
          "description": "CSV source path or open file object.",
          "values": [
            "'sales.csv'",
            "Path('data/orders.csv')"
          ],
          "pitfalls": [
            "Wrong relative path is the most common failure"
          ]
        },
        {
          "name": "usecols",
          "type": "list-like | callable",
          "description": "Subset columns to load.",
          "values": [
            "['order_id', 'amount']",
            "lambda c: c.startswith('metric_')"
          ],
          "pitfalls": [
            "Missing requested column raises error"
          ]
        },
        {
          "name": "dtype",
          "type": "dict[str, type] | type",
          "description": "Output dtype for `pandas.read_csv`. It controls memory footprint, precision, and interoperability with downstream steps.",
          "values": [
            "{'store_id': 'string'}",
            "{'amount': 'float64'}"
          ],
          "pitfalls": [
            "Bad dtype assignment fails on dirty data"
          ]
        },
        {
          "name": "parse_dates",
          "type": "bool | list",
          "description": "Columns to parse as datetimes.",
          "values": [
            "['order_date']"
          ],
          "pitfalls": [
            "Mixed formats may produce object dtype"
          ]
        },
        {
          "name": "sep",
          "type": "Any",
          "description": "Parameter `sep` configures how `pandas.read_csv` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "'"
        },
        {
          "name": "'",
          "type": "Any",
          "description": "Parameter `'` configures how `pandas.read_csv` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "description": "Loaded table.",
        "shape": "(rows, columns)"
      },
      "examples": [
        {
          "title": "Load daily sales extract",
          "scenario": "Finance ingests exported daily orders for gross revenue reporting.",
          "code": "import pandas as pd\nfrom io import StringIO\ncsv_data = StringIO('order_id,order_date,amount\\n1,2026-03-01,120.5\\n2,2026-03-02,89.0')\ndf = pd.read_csv(csv_data, parse_dates=['order_date'])\nprint(df.dtypes.to_dict())",
          "expectedOutput": "{'order_id': dtype('int64'), 'order_date': datetime64[ns], 'amount': dtype('float64')}",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Apply pandas.read_csv with explicit parameters shown in the snippet, then verify shapes/dtypes.",
            "Transfer the same pattern to your full dataset and rerun your validation checks."
          ]
        },
        {
          "title": "pandas.read_csv additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = pd.read_csv(filepath_or_buffer, sep=',', usecols=None, dtype=None, pnp.array([1, 1, 2, 3])se_dates=None)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run pandas.read_csv again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Use chunksize for very large files to reduce memory pressure."
      ],
      "related": [
        "pandas-to-datetime",
        "pandas-head"
      ],
      "overview": "Read CSV files into a DataFrame. Use pandas.read_csv when ingesting exports and serializing clean outputs. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: filepath_or_buffer, usecols, dtype. Returns pandas.DataFrame with shape (rows, columns).",
      "applications": [
        "Primary application: pandas.read_csv is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ]
    },
    {
      "id": "pandas-head",
      "slug": "pandas-dataframe-head",
      "name": "DataFrame.head",
      "library": "pandas",
      "subtopic": "I/O",
      "tags": [
        "inspect",
        "preview"
      ],
      "level": "core",
      "summary": "Return first n rows for quick inspection.",
      "syntax": "DataFrame.head(n=5)",
      "description": "Return first n rows for quick inspection. Use it for ingesting exports and serializing clean outputs and cleaning and reshaping transaction/event tables. Key parameter: n.",
      "docs": [
        {
          "label": "DataFrame.head",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.head.html"
        }
      ],
      "parameters": [
        {
          "name": "n",
          "type": "int",
          "default": "5",
          "description": "Number of rows to return.",
          "values": [
            "5",
            "10",
            "20"
          ],
          "pitfalls": [
            "Does not guarantee representative sample"
          ]
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "description": "Top n rows.",
        "shape": "(n, columns)"
      },
      "examples": [
        {
          "title": "DataFrame.head primary workflow",
          "scenario": "DataFrame.head used in a realistic business workflow with active command execution.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = DataFrame.head(n=5)\nprint(type(result).__name__)",
          "expectedOutput": "Executable output object/value that confirms the command ran on the sample dataset.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run DataFrame.head exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        },
        {
          "title": "DataFrame.head additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = DataFrame.head(n=5)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.head again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Use sample() for random previews."
      ],
      "related": [
        "pandas-read-csv"
      ],
      "overview": "Return first n rows for quick inspection. Use DataFrame.head when ingesting exports and serializing clean outputs. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameter: n. Returns pandas.DataFrame with shape (n, columns).",
      "applications": [
        "Primary application: DataFrame.head is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ]
    },
    {
      "id": "pandas-loc",
      "slug": "pandas-dataframe-loc",
      "name": "DataFrame.loc",
      "library": "pandas",
      "subtopic": "Selection",
      "tags": [
        "selection",
        "indexing"
      ],
      "level": "core",
      "summary": "Label-based row/column selection.",
      "syntax": "df.loc[row_selector, col_selector]",
      "description": "Label-based row/column selection. Use it for filtering and selecting numeric values efficiently and cleaning and reshaping transaction/event tables. Key parameters: row_selector, col_selector.",
      "docs": [
        {
          "label": "DataFrame.loc",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.loc.html"
        }
      ],
      "parameters": [
        {
          "name": "row_selector",
          "type": "label | list-like | bool mask | slice",
          "description": "Parameter `row_selector` configures how `DataFrame.loc` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "df['amount'] > 100",
            "'2026-03-01':'2026-03-10'"
          ],
          "pitfalls": [
            "Boolean mask must align with DataFrame index"
          ]
        },
        {
          "name": "col_selector",
          "type": "label | list-like | slice",
          "description": "Columns to select.",
          "values": [
            "'amount'",
            "['region','amount']"
          ],
          "pitfalls": [
            "KeyError for missing column labels"
          ]
        }
      ],
      "returns": {
        "type": "pandas.Series | pandas.DataFrame",
        "description": "Selected data view/copy depending on operation.",
        "shape": "depends on selectors"
      },
      "examples": [
        {
          "title": "DataFrame.loc primary workflow",
          "scenario": "DataFrame.loc used in a realistic business workflow with active command execution.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.loc[row_selector, col_selector]\nprint(type(result).__name__)",
          "expectedOutput": "Executable output object/value that confirms the command ran on the sample dataset.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run DataFrame.loc exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        },
        {
          "title": "DataFrame.loc additional business case",
          "scenario": "Second realistic usage variant where the same command is applied to a different data slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.loc[row_selector, col_selector]\nprint(type(result).__name__)",
          "expectedOutput": "A second executable output pattern that still uses the documented command.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run DataFrame.loc exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        }
      ],
      "notes": [
        "Use .copy() after slicing when you plan to mutate."
      ],
      "related": [
        "pandas-query",
        "pandas-assign"
      ],
      "overview": "Label-based row/column selection. Use DataFrame.loc when filtering and selecting numeric values efficiently. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: row_selector, col_selector. Returns pandas.Series | pandas.DataFrame with shape depends on selectors.",
      "applications": [
        "Primary application: DataFrame.loc is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ]
    },
    {
      "id": "pandas-assign",
      "slug": "pandas-dataframe-assign",
      "name": "DataFrame.assign",
      "library": "pandas",
      "subtopic": "Transformation",
      "tags": [
        "feature-engineering",
        "columns"
      ],
      "level": "core",
      "summary": "Return new DataFrame with added/updated columns.",
      "syntax": "df.assign(new_col=expr, other=lambda x: ...)",
      "description": "Return new DataFrame with added/updated columns. Use it for cleaning and engineering columns in tabular datasets and cleaning and reshaping transaction/event tables. Key parameter: **kwargs.",
      "docs": [
        {
          "label": "DataFrame.assign",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.assign.html"
        }
      ],
      "parameters": [
        {
          "name": "**kwargs",
          "type": "column expressions",
          "description": "Name-expression mapping for new columns.",
          "values": [
            "margin=df['rev']-df['cost']",
            "ratio=lambda x: x.a/x.b"
          ],
          "pitfalls": [
            "Later expressions can reference earlier assigned columns only in order"
          ]
        },
        {
          "name": "new_col",
          "type": "str | list[str]",
          "description": "Parameter `new_col` configures how `DataFrame.assign` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "'auto'",
            "'left'",
            "'mean'"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "expr"
        },
        {
          "name": "other",
          "type": "Any",
          "description": "Parameter `other` configures how `DataFrame.assign` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "lambda x: ..."
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "description": "New DataFrame with derived columns.",
        "shape": "(rows, columns + new_cols)"
      },
      "examples": [
        {
          "title": "Create contribution margin column",
          "scenario": "Finance adds margin and margin_pct to order table.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'revenue':[100,140], 'cost':[70,95]})\nout = df.assign(margin=lambda x: x['revenue']-x['cost'], margin_pct=lambda x: x['margin']/x['revenue'])\nprint(out[['margin','margin_pct']])",
          "expectedOutput": "2-row DataFrame with margin and margin_pct",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Apply DataFrame.assign with explicit parameters shown in the snippet, then verify shapes/dtypes.",
            "Transfer the same pattern to your full dataset and rerun your validation checks."
          ]
        },
        {
          "title": "DataFrame.assign additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.assign(new_col=expr, other=lambda x: ...)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.assign again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Prefer assign in chain pipelines for traceability."
      ],
      "related": [
        "pandas-query",
        "pandas-groupby-agg"
      ],
      "overview": "Return new DataFrame with added/updated columns. Use DataFrame.assign when cleaning and engineering columns in tabular datasets. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameter: **kwargs. Returns pandas.DataFrame with shape (rows, columns + new_cols).",
      "applications": [
        "Primary application: DataFrame.assign is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ]
    },
    {
      "id": "pandas-query",
      "slug": "pandas-dataframe-query",
      "name": "DataFrame.query",
      "library": "pandas",
      "subtopic": "Selection",
      "tags": [
        "filter",
        "expression"
      ],
      "level": "core",
      "summary": "Filter rows using expression strings.",
      "syntax": "df.query(expr, engine='numexpr')",
      "description": "Filter rows using expression strings. Use it for filtering and selecting numeric values efficiently and cleaning and reshaping transaction/event tables. Key parameters: expr, engine.",
      "docs": [
        {
          "label": "DataFrame.query",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.query.html"
        }
      ],
      "parameters": [
        {
          "name": "expr",
          "type": "str",
          "description": "Boolean expression over columns.",
          "values": [
            "'amount > 100 and region == \"N\"'"
          ],
          "pitfalls": [
            "Column names with spaces require backticks"
          ]
        },
        {
          "name": "engine",
          "type": "str",
          "default": "'numexpr'",
          "description": "Expression evaluation engine.",
          "values": [
            "'numexpr'",
            "'python'"
          ],
          "pitfalls": [
            "numexpr may not support all Python operations"
          ]
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "description": "Filtered rows.",
        "shape": "(<=rows, columns)"
      },
      "examples": [
        {
          "title": "Find at-risk subscriptions",
          "scenario": "SaaS team filters customers with low engagement and high invoice age.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'engagement':[0.2,0.8,0.3], 'invoice_days':[45,10,60]})\nout = df.query('engagement < 0.4 and invoice_days >= 30')\nprint(out.shape)",
          "expectedOutput": "(2, 2)",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Apply DataFrame.query with explicit parameters shown in the snippet, then verify shapes/dtypes.",
            "Transfer the same pattern to your full dataset and rerun your validation checks."
          ]
        },
        {
          "title": "DataFrame.query additional business case",
          "scenario": "Second realistic usage variant where the same command is applied to a different data slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.query(expr, engine='numexpr')\nprint(type(result).__name__)",
          "expectedOutput": "A second executable output pattern that still uses the documented command.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run DataFrame.query exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        }
      ],
      "notes": [
        "Use @var syntax to reference Python variables in query strings."
      ],
      "related": [
        "pandas-loc"
      ],
      "overview": "Filter rows using expression strings. Use DataFrame.query when filtering and selecting numeric values efficiently. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: expr, engine. Returns pandas.DataFrame with shape (<=rows, columns).",
      "applications": [
        "Primary application: DataFrame.query is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ]
    },
    {
      "id": "pandas-merge",
      "slug": "pandas-dataframe-merge",
      "name": "DataFrame.merge",
      "library": "pandas",
      "subtopic": "Join",
      "tags": [
        "join",
        "sql",
        "combine"
      ],
      "level": "core",
      "summary": "Database-style join between DataFrames.",
      "syntax": "df_left.merge(df_right, how='inner', on=None, left_on=None, right_on=None)",
      "description": "Database-style join between DataFrames. Use it for combining fact tables with lookup dimensions and combining transactional tables with customer/product reference data. Key parameters: how, on / left_on / right_on, validate.",
      "docs": [
        {
          "label": "DataFrame.merge",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.merge.html"
        }
      ],
      "parameters": [
        {
          "name": "how",
          "type": "{'left','right','inner','outer','cross'}",
          "default": "'inner'",
          "description": "Join/merge strategy for `DataFrame.merge`. It determines which keys are retained from each side of the join.",
          "values": [
            "left",
            "inner",
            "outer"
          ],
          "pitfalls": [
            "Many-to-many joins can explode row counts"
          ]
        },
        {
          "name": "on / left_on / right_on",
          "type": "str | list[str]",
          "description": "Parameter `on / left_on / right_on` configures how `DataFrame.merge` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "'customer_id'",
            "['store_id','week']"
          ],
          "pitfalls": [
            "Key dtype mismatch yields unexpected unmatched rows"
          ]
        },
        {
          "name": "validate",
          "type": "str",
          "description": "Join cardinality check.",
          "values": [
            "'1:1'",
            "'1:m'",
            "'m:1'"
          ],
          "pitfalls": [
            "Skipping validate can hide data quality issues"
          ]
        },
        {
          "name": "df_right",
          "type": "Any",
          "description": "Parameter `df_right` configures how `DataFrame.merge` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "on",
          "type": "Any",
          "description": "Column/key selector for `DataFrame.merge`. It controls grouping, joining, encoding, or styling based on the selected fields.",
          "values": [
            "single column name",
            "list of column names",
            "None (all applicable columns)"
          ],
          "pitfalls": [
            "Misspelled column names cause runtime errors.",
            "High-cardinality keys may increase memory and runtime."
          ],
          "default": "None"
        },
        {
          "name": "left_on",
          "type": "Any",
          "description": "Parameter `left_on` configures how `DataFrame.merge` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "None"
        },
        {
          "name": "right_on",
          "type": "Any",
          "description": "Parameter `right_on` configures how `DataFrame.merge` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "None"
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "description": "Joined table.",
        "shape": "depends on join cardinality"
      },
      "examples": [
        {
          "title": "DataFrame.merge primary workflow",
          "scenario": "DataFrame.merge used in a realistic business workflow with active command execution.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df_left.merge(df_right, how='inner', on=None, left_on=None, right_on=None)\nprint(type(result).__name__)",
          "expectedOutput": "Executable output object/value that confirms the command ran on the sample dataset.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run DataFrame.merge exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        },
        {
          "title": "DataFrame.merge additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df_left.merge(df_right, how='inner', on=None, left_on=None, right_on=None)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.merge again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Add indicator=True when auditing unmatched keys."
      ],
      "related": [
        "pandas-groupby-agg",
        "pandas-pivot-table"
      ],
      "overview": "Database-style join between DataFrames. Use DataFrame.merge when combining fact tables with lookup dimensions. Typical business pattern: combining transactional tables with customer/product reference data. Key parameters: how, on / left_on / right_on, validate. Returns pandas.DataFrame with shape depends on join cardinality.",
      "applications": [
        "Primary application: DataFrame.merge is used for combining transactional tables with customer/product reference data.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ]
    },
    {
      "id": "pandas-groupby-agg",
      "name": "GroupBy.agg (Core)",
      "subtopic": "Aggregation",
      "tags": [
        "groupby",
        "aggregation",
        "kpi"
      ],
      "level": "core",
      "summary": "Build KPI tables with one row per segment using explicit metric aggregations.",
      "syntax": "kpi=df.groupby('region',as_index=False).agg(total_revenue=('revenue','sum'),orders=('order_id','nunique'))",
      "description": "Groups rows by business grain and computes summary metrics for reporting and EDA.",
      "overview": "Build KPI tables with one row per segment using explicit metric aggregations. Use GroupBy.agg to move from event-level rows to decision-ready summaries by region/channel/product/cohort. Typical pattern: total revenue, average order, order count, unique customers. Key parameters: by, as_index, agg mapping. Returns compact KPI DataFrame.",
      "applications": [
        "Primary application: convert transactional data into segment-level scorecards.",
        "Workflow application: use named aggregations so metrics stay stable across runs.",
        "Delivery application: export grouped KPI tables directly to dashboards/stakeholder docs."
      ],
      "docs": [
        {
          "label": "DataFrameGroupBy.agg",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.core.groupby.DataFrameGroupBy.agg.html"
        }
      ],
      "parameters": [
        {
          "name": "by",
          "type": "str | list[str]",
          "description": "Grouping keys defining analysis grain.",
          "values": [
            "'region'",
            "['month','channel']"
          ],
          "pitfalls": [
            "Wrong grain can over/under-count KPIs."
          ]
        },
        {
          "name": "agg mapping",
          "type": "named aggregation mapping",
          "description": "Output metric definitions.",
          "values": [
            "total_revenue=('revenue','sum')",
            "orders=('order_id','nunique')"
          ],
          "pitfalls": [
            "Unclear metric names make interpretation difficult."
          ]
        },
        {
          "name": "'region'",
          "type": "Any",
          "description": "Parameter `'region'` configures how `GroupBy.agg (Core)` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "as_index",
          "type": "Any",
          "description": "Parameter `as_index` configures how `GroupBy.agg (Core)` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "False).agg(total_revenue=('revenue'"
        },
        {
          "name": "'sum')",
          "type": "Any",
          "description": "Parameter `'sum')` configures how `GroupBy.agg (Core)` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "orders",
          "type": "Any",
          "description": "Parameter `orders` configures how `GroupBy.agg (Core)` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "('order_id'"
        },
        {
          "name": "'nunique')",
          "type": "Any",
          "description": "Parameter `'nunique')` configures how `GroupBy.agg (Core)` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "shape": "(groups, metrics + key cols)",
        "description": "Grouped summary table for EDA and reporting."
      },
      "examples": [
        {
          "title": "Region KPI scorecard",
          "scenario": "Analyst needs total and average performance by region for weekly review.",
          "code": "import pandas as pd\n\ndf=pd.DataFrame({'region':['W','W','E','E'],'order_id':[1,2,3,4],'revenue':[120,80,95,140]})\nkpi=df.groupby('region',as_index=False).agg(total_revenue=('revenue','sum'),avg_order=('revenue','mean'),orders=('order_id','nunique'))\nprint(kpi)",
          "expectedOutput": "One row per region with explicit KPI columns.",
          "walkthrough": [
            "Choose grouping dimension matching the business question.",
            "Define named metrics in agg so output is report-ready.",
            "Validate row counts and metric logic before publishing."
          ]
        }
      ],
      "notes": [
        "Named aggregation is the cleanest default for production summaries."
      ],
      "related": [
        "pandas-groupby-basic",
        "pandas-groupby-named-agg",
        "pandas-pivot-table"
      ],
      "slug": "pandas-groupby-agg",
      "library": "pandas"
    },
    {
      "id": "pandas-pivot-table",
      "slug": "pandas-pivot-table",
      "name": "DataFrame.pivot_table",
      "library": "pandas",
      "subtopic": "Aggregation",
      "tags": [
        "pivot",
        "matrix",
        "summary"
      ],
      "level": "core",
      "summary": "Create spreadsheet-style aggregated pivot tables.",
      "syntax": "df.pivot_table(values, index, columns=None, aggfunc='mean', fill_value=None)",
      "description": "Create spreadsheet-style aggregated pivot tables. Use it for building grouped KPI tables for reporting and cleaning and reshaping transaction/event tables. Key parameters: values, index, columns, aggfunc.",
      "docs": [
        {
          "label": "DataFrame.pivot_table",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.pivot_table.html"
        }
      ],
      "parameters": [
        {
          "name": "values",
          "type": "str | list[str]",
          "description": "Metric column(s) to aggregate.",
          "values": [
            "'revenue'",
            "['revenue','orders']"
          ],
          "pitfalls": [
            "Non-numeric values need compatible aggfunc"
          ]
        },
        {
          "name": "index, columns",
          "type": "str | list[str]",
          "description": "Row/column grouping keys.",
          "values": [
            "index='region', columns='month'"
          ],
          "pitfalls": [
            "Large cardinality creates very wide pivot"
          ]
        },
        {
          "name": "aggfunc",
          "type": "str | function | list",
          "default": "'mean'",
          "description": "Aggregation function(s).",
          "values": [
            "'sum'",
            "'mean'",
            "'count'"
          ],
          "pitfalls": [
            "Multiple aggfunc produce MultiIndex columns"
          ]
        },
        {
          "name": "index",
          "type": "Any",
          "description": "Column/key selector for `DataFrame.pivot_table`. It controls grouping, joining, encoding, or styling based on the selected fields.",
          "values": [
            "single column name",
            "list of column names",
            "None (all applicable columns)"
          ],
          "pitfalls": [
            "Misspelled column names cause runtime errors.",
            "High-cardinality keys may increase memory and runtime."
          ]
        },
        {
          "name": "columns",
          "type": "str | list[str]",
          "description": "Columns included in this transformation step.",
          "values": [
            "'auto'",
            "'left'",
            "'mean'"
          ],
          "pitfalls": [
            "Confirm required columns exist before running.",
            "Check dtypes so numeric operations/modeling behave as expected."
          ],
          "default": "None"
        },
        {
          "name": "fill_value",
          "type": "Any",
          "description": "Parameter `fill_value` configures how `DataFrame.pivot_table` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "None"
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "description": "Pivoted summary matrix.",
        "shape": "(len(index groups), len(column groups))"
      },
      "examples": [
        {
          "title": "Region x channel revenue matrix",
          "scenario": "Leadership review needs compact matrix for sales distribution.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'region':['N','N','S'], 'channel':['ads','organic','ads'], 'revenue':[120,90,140]})\npvt = df.pivot_table(values='revenue', index='region', columns='channel', aggfunc='sum', fill_value=0)\nprint(pvt)",
          "expectedOutput": "2x2 pivot with summed revenue values",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Apply DataFrame.pivot_table with explicit parameters shown in the snippet, then verify shapes/dtypes.",
            "Transfer the same pattern to your full dataset and rerun your validation checks."
          ]
        },
        {
          "title": "DataFrame.pivot_table additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.pivot_table(records[0]s, index, columns=None, aggfunc='mean', 0.5=None)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.pivot_table again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Use fill_value to avoid NaNs in dashboard exports."
      ],
      "related": [
        "pandas-groupby-agg"
      ],
      "overview": "Create spreadsheet-style aggregated pivot tables. Use DataFrame.pivot_table when building grouped KPI tables for reporting. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: values, index, columns, aggfunc. Returns pandas.DataFrame with shape (len(index groups), len(column groups)).",
      "applications": [
        "Primary application: DataFrame.pivot_table is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ]
    },
    {
      "id": "pandas-sort-values",
      "slug": "pandas-sort-values",
      "name": "DataFrame.sort_values",
      "library": "pandas",
      "subtopic": "Transformation",
      "tags": [
        "sorting",
        "ranking"
      ],
      "level": "core",
      "summary": "Sort rows by one or more columns.",
      "syntax": "df.sort_values(by, ascending=True, na_position='last')",
      "description": "Sort rows by one or more columns. Use it for cleaning and engineering columns in tabular datasets and cleaning and reshaping transaction/event tables. Key parameters: by, ascending, na_position.",
      "docs": [
        {
          "label": "DataFrame.sort_values",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.sort_values.html"
        }
      ],
      "parameters": [
        {
          "name": "by",
          "type": "str | list[str]",
          "description": "Column/key selector for `DataFrame.sort_values`. It controls grouping, joining, encoding, or styling based on the selected fields.",
          "values": [
            "'revenue'",
            "['region','revenue']"
          ],
          "pitfalls": [
            "Mixed dtypes can produce unexpected ordering"
          ]
        },
        {
          "name": "ascending",
          "type": "bool | list[bool]",
          "default": "True",
          "description": "Execution behavior flag for `DataFrame.sort_values`. It changes orientation, ordering, mutability, or null-handling of the operation.",
          "values": [
            "True",
            "False",
            "[True, False]"
          ],
          "pitfalls": [
            "List length must match number of by columns"
          ]
        },
        {
          "name": "na_position",
          "type": "{'first','last'}",
          "default": "'last'",
          "description": "Where to place missing values.",
          "values": [
            "'first'",
            "'last'"
          ],
          "pitfalls": [
            "Default can hide missing rows at bottom"
          ]
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "description": "Sorted DataFrame.",
        "shape": "same as input"
      },
      "examples": [
        {
          "title": "Top 3 risky accounts",
          "scenario": "Risk ops sorts accounts by descending risk score.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'account':[101,102,103], 'risk_score':[0.12,0.82,0.55]})\nprint(df.sort_values('risk_score', ascending=False).head(3))",
          "expectedOutput": "Rows ordered by risk_score: 102, 103, 101",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Apply DataFrame.sort_values with explicit parameters shown in the snippet, then verify shapes/dtypes.",
            "Transfer the same pattern to your full dataset and rerun your validation checks."
          ]
        },
        {
          "title": "DataFrame.sort_values additional business case",
          "scenario": "Second realistic usage variant where the same command is applied to a different data slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.sort_values(by, ascending=True, na_position='last')\nprint(type(result).__name__)",
          "expectedOutput": "A second executable output pattern that still uses the documented command.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run DataFrame.sort_values exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        }
      ],
      "notes": [
        "Use stable mergesort when deterministic order on ties matters."
      ],
      "related": [
        "pandas-head"
      ],
      "overview": "Sort rows by one or more columns. Use DataFrame.sort_values when cleaning and engineering columns in tabular datasets. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: by, ascending, na_position. Returns pandas.DataFrame with shape same as input.",
      "applications": [
        "Primary application: DataFrame.sort_values is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ]
    },
    {
      "id": "pandas-fillna",
      "slug": "pandas-fillna",
      "name": "DataFrame.fillna",
      "library": "pandas",
      "subtopic": "Missing Data",
      "tags": [
        "missing",
        "imputation"
      ],
      "level": "core",
      "summary": "Fill missing values with scalar, dict, or method.",
      "syntax": "df.fillna(value=None, method=None, axis=None, limit=None)",
      "description": "Fill missing values with scalar, dict, or method. Use it for auditing and remediating null-heavy source extracts and cleaning and reshaping transaction/event tables. Key parameters: value, method, limit.",
      "docs": [
        {
          "label": "DataFrame.fillna",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.fillna.html"
        }
      ],
      "parameters": [
        {
          "name": "value",
          "type": "scalar | dict | Series | DataFrame",
          "description": "Replacement value(s).",
          "values": [
            "0",
            "{'region':'unknown'}"
          ],
          "pitfalls": [
            "Column dtype may change after fill"
          ]
        },
        {
          "name": "method",
          "type": "{'ffill','bfill'}",
          "description": "Propagation strategy.",
          "values": [
            "'ffill'",
            "'bfill'"
          ],
          "pitfalls": [
            "Method can leak future info in ML if misused"
          ]
        },
        {
          "name": "limit",
          "type": "int",
          "description": "Max consecutive NaNs to fill.",
          "values": [
            "1",
            "2",
            "5"
          ],
          "pitfalls": [
            "Not all missing gaps will be filled"
          ]
        },
        {
          "name": "axis",
          "type": "int",
          "description": "Axis along which operation is applied.",
          "values": [
            "1",
            "5",
            "10"
          ],
          "pitfalls": [
            "Wrong axis silently changes calculation direction and interpretation."
          ],
          "default": "None"
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "description": "DataFrame with filled values.",
        "shape": "same as input"
      },
      "examples": [
        {
          "title": "Fill missing campaign spend",
          "scenario": "Marketing report requires zero for missing spend cells.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'campaign':['A','B'], 'spend':[1200, None]})\nout = df.fillna({'spend': 0})\nprint(out)",
          "expectedOutput": "Spend for campaign B becomes 0",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Apply DataFrame.fillna with explicit parameters shown in the snippet, then verify shapes/dtypes.",
            "Transfer the same pattern to your full dataset and rerun your validation checks."
          ]
        },
        {
          "title": "DataFrame.fillna additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.fillna(records[0]=None, method=None, axis=None, limit=None)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.fillna again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Consider model-based imputation for production ML pipelines."
      ],
      "related": [
        "pandas-groupby-agg",
        "np-isnan"
      ],
      "overview": "Fill missing values with scalar, dict, or method. Use DataFrame.fillna when auditing and remediating null-heavy source extracts. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: value, method, limit. Returns pandas.DataFrame with shape same as input.",
      "applications": [
        "Primary application: DataFrame.fillna is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ]
    },
    {
      "id": "pandas-to-datetime",
      "name": "pandas.to_datetime",
      "subtopic": "Time Series",
      "tags": [
        "datetime",
        "parsing",
        "eda"
      ],
      "level": "core",
      "summary": "Parse raw date columns to datetime dtype before any temporal analysis.",
      "syntax": "df['order_date']=pd.to_datetime(df['order_date_raw'], errors='coerce')",
      "description": "Converts source date fields into datetime so dt.year filtering and resampling work reliably.",
      "overview": "Parse raw date columns to datetime dtype before any temporal analysis. Use pandas.to_datetime immediately after ingestion so downstream dt accessors, date filters, and resampling are consistent. Typical pattern: parse, audit parse nulls, then derive calendar features. Key parameters: arg, format, errors, utc. Returns datetime-aligned series/index.",
      "applications": [
        "Primary application: standardize incoming date columns from CSV/Excel/warehouse exports.",
        "Workflow application: parse once early and reuse dt features across EDA/modeling.",
        "Delivery application: track parse-failure rate so assumptions are explicit."
      ],
      "docs": [
        {
          "label": "pandas.to_datetime",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.to_datetime.html"
        }
      ],
      "parameters": [
        {
          "name": "arg",
          "type": "Series | list | scalar",
          "description": "Raw datetime-like values to parse.",
          "values": [
            "df['order_date_raw']",
            "['2026-01-01','2026-01-02']"
          ],
          "pitfalls": [
            "Mixed formats can produce NaT rows when errors='coerce'."
          ]
        },
        {
          "name": "errors",
          "type": "str",
          "default": "'raise'",
          "description": "Failure handling policy.",
          "values": [
            "'raise'",
            "'coerce'",
            "'ignore'"
          ],
          "pitfalls": [
            "'ignore' can leave object dtype and break dt accessors."
          ]
        },
        {
          "name": "df['order_date_raw']",
          "type": "Any",
          "description": "Parameter `df['order_date_raw']` configures how `pandas.to_datetime` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "pandas.Series | pandas.DatetimeIndex",
        "shape": "same length as input",
        "description": "Datetime-converted values ready for date-based analysis."
      },
      "examples": [
        {
          "title": "Parse and audit date field",
          "scenario": "New dataset includes inconsistent date strings and needs safe parsing before EDA.",
          "code": "import pandas as pd\n\ndf=pd.DataFrame({'order_date_raw':['2026-01-01','01/05/2026','bad'],'revenue':[120,95,70]})\ndf['order_date']=pd.to_datetime(df['order_date_raw'],errors='coerce')\nprint(df)\nprint('parse_null_rate',df['order_date'].isna().mean())",
          "expectedOutput": "Datetime column created, invalid rows become NaT, and parse null rate is visible.",
          "walkthrough": [
            "Parse the raw date field with an explicit errors policy.",
            "Check dtype and parse null rate immediately.",
            "Proceed to dt.year/groupby/resample only after validation."
          ]
        }
      ],
      "notes": [
        "Set format when source format is fixed for speed and fewer parse surprises."
      ],
      "related": [
        "pandas-dt-year",
        "pandas-dt-components",
        "pandas-resample"
      ],
      "slug": "pandas-to-datetime",
      "library": "pandas"
    },
    {
      "id": "pandas-resample",
      "slug": "pandas-dataframe-resample",
      "name": "DataFrame.resample",
      "library": "pandas",
      "subtopic": "Time Series",
      "tags": [
        "timeseries",
        "resample",
        "aggregation"
      ],
      "level": "core",
      "summary": "Resample time-indexed data into new frequency bins.",
      "syntax": "df.resample(rule, on=None).agg(func)",
      "description": "Resample time-indexed data into new frequency bins. Use it for constructing lag, rolling, and resampled temporal features and summarizing raw events into decision-ready KPIs. Key parameters: rule, on, func.",
      "docs": [
        {
          "label": "DataFrame.resample",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.resample.html"
        }
      ],
      "parameters": [
        {
          "name": "rule",
          "type": "offset alias",
          "description": "Parameter `rule` configures how `DataFrame.resample` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "'D'",
            "'W'",
            "'M'",
            "'H'"
          ],
          "pitfalls": [
            "Month-end defaults may differ from business definitions"
          ]
        },
        {
          "name": "on",
          "type": "str",
          "description": "Datetime column if index is not datetime.",
          "values": [
            "'event_time'"
          ],
          "pitfalls": [
            "Column must be datetime-like"
          ]
        },
        {
          "name": "func",
          "type": "str | function",
          "description": "Aggregation applied per bin.",
          "values": [
            "'sum'",
            "'count'",
            "'mean'"
          ],
          "pitfalls": [
            "Missing bins may need fillna after resample"
          ]
        }
      ],
      "returns": {
        "type": "pandas.Series | pandas.DataFrame",
        "description": "Time-bucketed aggregation.",
        "shape": "(num_bins, metrics)"
      },
      "examples": [
        {
          "title": "Weekly GMV trend",
          "scenario": "E-commerce dashboard rolls transaction-level GMV into weekly totals.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'ts':['2026-01-01','2026-01-02','2026-01-08'], 'gmv':[120,80,200]})\ndf['ts'] = pd.to_datetime(df['ts'])\nweekly = df.resample('W', on='ts').agg(total_gmv=('gmv','sum'))\nprint(weekly.shape)",
          "expectedOutput": "(2, 1)",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Apply DataFrame.resample with explicit parameters shown in the snippet, then verify shapes/dtypes.",
            "Transfer the same pattern to your full dataset and rerun your validation checks."
          ]
        },
        {
          "title": "DataFrame.resample additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.resample(rule, on=None).agg(func)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.resample again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Ensure timezone consistency before resampling global event data."
      ],
      "related": [
        "pandas-to-datetime",
        "pandas-groupby-agg"
      ],
      "overview": "Resample time-indexed data into new frequency bins. Use DataFrame.resample when constructing lag, rolling, and resampled temporal features. Typical business pattern: summarizing raw events into decision-ready KPIs. Key parameters: rule, on, func. Returns pandas.Series | pandas.DataFrame with shape (num_bins, metrics).",
      "applications": [
        "Primary application: DataFrame.resample is used for summarizing raw events into decision-ready KPIs.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ]
    },
    {
      "id": "pandas-read-excel",
      "slug": "pandas-read-excel",
      "name": "pandas.read_excel",
      "library": "pandas",
      "subtopic": "I/O",
      "tags": [
        "excel",
        "io",
        "loading"
      ],
      "level": "extended",
      "summary": "pandas.read_excel loads worksheet data into a DataFrame.",
      "syntax": "pd.read_excel(io, sheet_name=0, usecols=None)",
      "description": "pandas.read_excel loads worksheet data into a DataFrame. Use it for ingesting exports and serializing clean outputs and cleaning and reshaping transaction/event tables. Key parameters: io, sheet_name, usecols.",
      "overview": "pandas.read_excel loads worksheet data into a DataFrame. Use pandas.read_excel when ingesting exports and serializing clean outputs. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: io, sheet_name, usecols. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: pandas.read_excel is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "pandas.read_excel official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.read_excel.html"
        }
      ],
      "parameters": [
        {
          "name": "io",
          "type": "Any",
          "description": "Parameter `io` configures how `pandas.read_excel` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "sheet_name",
          "type": "Any",
          "default": "0",
          "description": "Parameter `sheet_name` configures how `pandas.read_excel` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "usecols",
          "type": "str | list[str]",
          "default": "None",
          "description": "Column/key selector for `pandas.read_excel`. It controls grouping, joining, encoding, or styling based on the selected fields.",
          "values": [
            "'auto'",
            "'left'",
            "'mean'"
          ],
          "pitfalls": [
            "Misspelled column names cause runtime errors.",
            "High-cardinality keys may increase memory and runtime."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "pandas.read_excel for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = pd.read_excel(io, sheet_name=0, usecols=None)\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run pandas.read_excel directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "pandas.read_excel additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = pd.read_excel(io, sheet_name=0, usecols=None)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run pandas.read_excel again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-info",
      "slug": "pandas-info",
      "name": "DataFrame.info",
      "library": "pandas",
      "subtopic": "I/O",
      "tags": [
        "inspection",
        "schema"
      ],
      "level": "extended",
      "summary": "DataFrame.info prints schema, non-null counts, and memory usage.",
      "syntax": "df.info(verbose=None, show_counts=None)",
      "description": "DataFrame.info prints schema, non-null counts, and memory usage. Use it for ingesting exports and serializing clean outputs and cleaning and reshaping transaction/event tables. Key parameters: verbose, show_counts.",
      "overview": "DataFrame.info prints schema, non-null counts, and memory usage. Use DataFrame.info when ingesting exports and serializing clean outputs. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: verbose, show_counts. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.info is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.info official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.info.html"
        }
      ],
      "parameters": [
        {
          "name": "verbose",
          "type": "Any",
          "default": "None",
          "description": "Parameter `verbose` configures how `DataFrame.info` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "show_counts",
          "type": "Any",
          "default": "None",
          "description": "Parameter `show_counts` configures how `DataFrame.info` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.info for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = df.info(verbose=None, show_counts=None)\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run DataFrame.info directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "DataFrame.info additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.info(verbose=None, show_counts=None)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.info again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-describe",
      "slug": "pandas-describe",
      "name": "DataFrame.describe",
      "library": "pandas",
      "subtopic": "Aggregation",
      "tags": [
        "eda",
        "summary"
      ],
      "level": "extended",
      "summary": "DataFrame.describe returns summary statistics for numeric and categorical columns.",
      "syntax": "df.describe(include=None)",
      "description": "DataFrame.describe returns summary statistics for numeric and categorical columns. Use it for building grouped KPI tables for reporting and cleaning and reshaping transaction/event tables. Key parameter: include.",
      "overview": "DataFrame.describe returns summary statistics for numeric and categorical columns. Use DataFrame.describe when building grouped KPI tables for reporting. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameter: include. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.describe is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.describe official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.describe.html"
        }
      ],
      "parameters": [
        {
          "name": "include",
          "type": "Any",
          "default": "None",
          "description": "Parameter `include` configures how `DataFrame.describe` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.describe for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = df.describe(include=None)\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run DataFrame.describe directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "DataFrame.describe additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.describe(include=None)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.describe again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-iloc",
      "slug": "pandas-iloc",
      "name": "DataFrame.iloc",
      "library": "pandas",
      "subtopic": "Selection",
      "tags": [
        "selection",
        "indexing"
      ],
      "level": "extended",
      "summary": "DataFrame.iloc performs integer-position based row/column selection.",
      "syntax": "df.iloc[row_indexer, col_indexer]",
      "description": "DataFrame.iloc performs integer-position based row/column selection. Use it for filtering and selecting numeric values efficiently and cleaning and reshaping transaction/event tables. Most controls are optional keyword arguments.",
      "overview": "DataFrame.iloc performs integer-position based row/column selection. Use DataFrame.iloc when filtering and selecting numeric values efficiently. Typical business pattern: cleaning and reshaping transaction/event tables. Most controls are optional keyword arguments. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.iloc is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.iloc official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.iloc.html"
        }
      ],
      "parameters": [
        {
          "name": "inputs",
          "type": "Any",
          "description": "Data input consumed by `DataFrame.iloc`. Changing this argument changes which rows/columns are processed and can alter output shape and statistics.",
          "values": [
            "DataFrame",
            "array-like object",
            "Series/list input"
          ],
          "pitfalls": [
            "Confirm required columns exist before running.",
            "Check dtypes so numeric operations/modeling behave as expected."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.iloc primary workflow",
          "scenario": "DataFrame.iloc used in a realistic business workflow with active command execution.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.iloc[:, :2]\nprint(type(result).__name__)",
          "expectedOutput": "Executable output object/value that confirms the command ran on the sample dataset.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run DataFrame.iloc exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        },
        {
          "title": "DataFrame.iloc additional business case",
          "scenario": "Second realistic usage variant where the same command is applied to a different data slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.iloc[:, :2]\nprint(type(result).__name__)",
          "expectedOutput": "A second executable output pattern that still uses the documented command.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run DataFrame.iloc exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-drop",
      "slug": "pandas-drop",
      "name": "DataFrame.drop",
      "library": "pandas",
      "subtopic": "Transformation",
      "tags": [
        "cleanup",
        "columns"
      ],
      "level": "extended",
      "summary": "DataFrame.drop removes rows or columns by labels.",
      "syntax": "df.drop(labels=None, axis=0, columns=None, inplace=False)",
      "description": "DataFrame.drop removes rows or columns by labels. Use it for cleaning and engineering columns in tabular datasets and cleaning and reshaping transaction/event tables. Key parameters: labels, axis, columns.",
      "overview": "DataFrame.drop removes rows or columns by labels. Use DataFrame.drop when cleaning and engineering columns in tabular datasets. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: labels, axis, columns. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.drop is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.drop official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.drop.html"
        }
      ],
      "parameters": [
        {
          "name": "labels",
          "type": "str | list[str]",
          "default": "None",
          "description": "Target or label values used by `DataFrame.drop`. This controls what the model/metric learns or evaluates against.",
          "values": [
            "'auto'",
            "'left'",
            "'mean'"
          ],
          "pitfalls": [
            "Ensure `X` and `y` have matching row counts.",
            "Avoid leakage by excluding post-outcome fields from features."
          ]
        },
        {
          "name": "axis",
          "type": "int",
          "default": "0",
          "description": "Axis along which operation is applied.",
          "values": [
            "1",
            "5",
            "10"
          ],
          "pitfalls": [
            "Wrong axis silently changes calculation direction and interpretation."
          ]
        },
        {
          "name": "columns",
          "type": "str | list[str]",
          "default": "None",
          "description": "Columns included in this transformation step.",
          "values": [
            "'auto'",
            "'left'",
            "'mean'"
          ],
          "pitfalls": [
            "Confirm required columns exist before running.",
            "Check dtypes so numeric operations/modeling behave as expected."
          ]
        },
        {
          "name": "inplace",
          "type": "bool",
          "default": "False",
          "description": "Execution behavior flag for `DataFrame.drop`. It changes orientation, ordering, mutability, or null-handling of the operation.",
          "values": [
            "True",
            "False"
          ],
          "pitfalls": [
            "Boolean default can materially change output semantics."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.drop for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = df.drop(labels=None, axis=0, columns=None, inplace=False)\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run DataFrame.drop directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "DataFrame.drop additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.drop(labels=None, axis=0, columns=None, inplace=False)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.drop again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-rename",
      "slug": "pandas-rename",
      "name": "DataFrame.rename",
      "library": "pandas",
      "subtopic": "Transformation",
      "tags": [
        "labels",
        "cleanup"
      ],
      "level": "extended",
      "summary": "DataFrame.rename changes row/column labels without mutating source by default.",
      "syntax": "df.rename(columns=None, index=None)",
      "description": "DataFrame.rename changes row/column labels without mutating source by default. Use it for cleaning and engineering columns in tabular datasets and cleaning and reshaping transaction/event tables. Key parameters: columns, index.",
      "overview": "DataFrame.rename changes row/column labels without mutating source by default. Use DataFrame.rename when cleaning and engineering columns in tabular datasets. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: columns, index. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.rename is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.rename official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.rename.html"
        }
      ],
      "parameters": [
        {
          "name": "columns",
          "type": "str | list[str]",
          "default": "None",
          "description": "Columns included in this transformation step.",
          "values": [
            "'auto'",
            "'left'",
            "'mean'"
          ],
          "pitfalls": [
            "Confirm required columns exist before running.",
            "Check dtypes so numeric operations/modeling behave as expected."
          ]
        },
        {
          "name": "index",
          "type": "Any",
          "default": "None",
          "description": "Column/key selector for `DataFrame.rename`. It controls grouping, joining, encoding, or styling based on the selected fields.",
          "values": [
            "single column name",
            "list of column names",
            "None (all applicable columns)"
          ],
          "pitfalls": [
            "Misspelled column names cause runtime errors.",
            "High-cardinality keys may increase memory and runtime."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.rename for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = df.rename(columns=None, index=None)\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run DataFrame.rename directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "DataFrame.rename additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.rename(columns=None, index=None)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.rename again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-astype",
      "slug": "pandas-astype",
      "name": "DataFrame.astype",
      "library": "pandas",
      "subtopic": "Transformation",
      "tags": [
        "types",
        "casting"
      ],
      "level": "extended",
      "summary": "DataFrame.astype casts columns to specific data types.",
      "syntax": "df.astype(dtype, copy=True)",
      "description": "DataFrame.astype casts columns to specific data types. Use it for cleaning and engineering columns in tabular datasets and cleaning and reshaping transaction/event tables. Key parameters: dtype, copy.",
      "overview": "DataFrame.astype casts columns to specific data types. Use DataFrame.astype when cleaning and engineering columns in tabular datasets. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: dtype, copy. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.astype is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.astype official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.astype.html"
        }
      ],
      "parameters": [
        {
          "name": "dtype",
          "type": "Any",
          "description": "Output dtype for `DataFrame.astype`. It controls memory footprint, precision, and interoperability with downstream steps.",
          "values": [
            "'float64'",
            "'int64'",
            "'int8' / 'category' where applicable"
          ],
          "pitfalls": [
            "Downcasting can truncate values or overflow.",
            "Mixed dtypes can break model pipelines and aggregations."
          ]
        },
        {
          "name": "copy",
          "type": "bool",
          "default": "True",
          "description": "Execution behavior flag for `DataFrame.astype`. It changes orientation, ordering, mutability, or null-handling of the operation.",
          "values": [
            "True",
            "False"
          ],
          "pitfalls": [
            "Boolean default can materially change output semantics."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.astype for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = df.astype(dtype, copy=True)\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run DataFrame.astype directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "DataFrame.astype additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.astype(dtype, copy=True)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.astype again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-value-counts",
      "slug": "pandas-value-counts",
      "name": "Series.value_counts",
      "library": "pandas",
      "subtopic": "Aggregation",
      "tags": [
        "counts",
        "categorical"
      ],
      "level": "extended",
      "summary": "Series.value_counts counts category frequency, optionally normalized.",
      "syntax": "s.value_counts(normalize=False, dropna=True)",
      "description": "Series.value_counts counts category frequency, optionally normalized. Use it for building grouped KPI tables for reporting and cleaning and reshaping transaction/event tables. Key parameters: normalize, dropna.",
      "overview": "Series.value_counts counts category frequency, optionally normalized. Use Series.value_counts when building grouped KPI tables for reporting. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: normalize, dropna. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: Series.value_counts is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "Series.value_counts official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.Series.value_counts.html"
        }
      ],
      "parameters": [
        {
          "name": "normalize",
          "type": "Any",
          "default": "False",
          "description": "Parameter `normalize` configures how `Series.value_counts` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "dropna",
          "type": "bool",
          "default": "True",
          "description": "Execution behavior flag for `Series.value_counts`. It changes orientation, ordering, mutability, or null-handling of the operation.",
          "values": [
            "True",
            "False"
          ],
          "pitfalls": [
            "Boolean default can materially change output semantics."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "Series.value_counts for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = s.value_counts(normalize=False, dropna=True)\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run Series.value_counts directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "Series.value_counts additional business case",
          "scenario": "Second realistic usage variant where the same command is applied to a different data slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = s.value_counts(normalize=False, dropna=True)\nprint(type(result).__name__)",
          "expectedOutput": "A second executable output pattern that still uses the documented command.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run Series.value_counts exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-groupby-transform",
      "name": "GroupBy.transform",
      "subtopic": "Aggregation",
      "tags": [
        "groupby",
        "transform",
        "feature-engineering"
      ],
      "level": "extended",
      "summary": "Create row-level features that depend on group context.",
      "syntax": "df['share_in_region']=df['revenue']/df.groupby('region')['revenue'].transform('sum')",
      "description": "Keeps original row count while adding group-relative features such as shares or z-scores.",
      "overview": "Create row-level features that depend on group context. Use transform when each row must be compared to its peer group without collapsing the table. Typical pattern: within-segment share, within-plan z-score, or deviation from group mean before modeling. Key parameters: by, function, target column. Returns row-aligned series/dataframe.",
      "applications": [
        "Primary application: generate relative features for EDA and model performance gains.",
        "Workflow application: use transform before train/test split so feature logic is transparent.",
        "Delivery application: explain derived group-relative metrics alongside raw values."
      ],
      "docs": [
        {
          "label": "SeriesGroupBy.transform",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.core.groupby.SeriesGroupBy.transform.html"
        }
      ],
      "parameters": [
        {
          "name": "by",
          "type": "str | list[str]",
          "description": "Grouping keys for comparison context.",
          "values": [
            "'region'",
            "['region','channel']"
          ],
          "pitfalls": [
            "Incorrect grouping key makes relative metrics misleading."
          ]
        },
        {
          "name": "func",
          "type": "str | callable",
          "description": "Groupwise transformation function.",
          "values": [
            "'sum'",
            "'mean'",
            "lambda s:(s-s.mean())/s.std(ddof=0)"
          ],
          "pitfalls": [
            "Complex lambdas can be slow on large data."
          ]
        },
        {
          "name": "'region')['revenue'].transform('sum'",
          "type": "Any",
          "description": "Parameter `'region')['revenue'].transform('sum'` configures how `GroupBy.transform` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "pandas.Series | pandas.DataFrame",
        "shape": "same rows as input",
        "description": "Row-aligned transformed feature(s)."
      },
      "examples": [
        {
          "title": "Share-of-region feature",
          "scenario": "Revenue model needs each order expressed as share of regional total.",
          "code": "import pandas as pd\n\ndf=pd.DataFrame({'region':['W','W','E','E'],'revenue':[120,80,90,110]})\ndf['share_in_region']=df['revenue']/df.groupby('region')['revenue'].transform('sum')\nprint(df)",
          "expectedOutput": "Each row gets a share feature; shares sum to 1 per region.",
          "walkthrough": [
            "Group by the comparison segment.",
            "Use transform to keep one value per original row.",
            "Validate share sums and reuse in downstream modeling."
          ]
        }
      ],
      "notes": [
        "Use agg instead when you want one row per group rather than row-level features."
      ],
      "related": [
        "pandas-groupby-agg",
        "pandas-groupby-share-total",
        "pandas-assign"
      ],
      "slug": "pandas-groupby-transform",
      "library": "pandas"
    },
    {
      "id": "pandas-melt",
      "slug": "pandas-melt",
      "name": "pandas.melt",
      "library": "pandas",
      "subtopic": "Transformation",
      "tags": [
        "reshape",
        "long-format"
      ],
      "level": "extended",
      "summary": "pandas.melt reshapes wide tables into long format.",
      "syntax": "pd.melt(frame, id_vars=None, value_vars=None)",
      "description": "pandas.melt reshapes wide tables into long format. Use it for cleaning and engineering columns in tabular datasets and cleaning and reshaping transaction/event tables. Key parameters: frame, id_vars, value_vars.",
      "overview": "pandas.melt reshapes wide tables into long format. Use pandas.melt when cleaning and engineering columns in tabular datasets. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: frame, id_vars, value_vars. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: pandas.melt is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "pandas.melt official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.melt.html"
        }
      ],
      "parameters": [
        {
          "name": "frame",
          "type": "Any",
          "description": "Parameter `frame` configures how `pandas.melt` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "id_vars",
          "type": "Any",
          "default": "None",
          "description": "Parameter `id_vars` configures how `pandas.melt` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "value_vars",
          "type": "Any",
          "default": "None",
          "description": "Parameter `value_vars` configures how `pandas.melt` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "pandas.melt for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = pd.melt(df, id_vars=['segment'], value_vars=['value'])\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run pandas.melt directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "pandas.melt additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = pd.melt(df, id_vnp.array([1, 1, 2, 3])s=['segment'], records[0]_vars=['value'])\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run pandas.melt again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-concat",
      "slug": "pandas-concat",
      "name": "pandas.concat",
      "library": "pandas",
      "subtopic": "Join",
      "tags": [
        "combine",
        "append"
      ],
      "level": "extended",
      "summary": "pandas.concat combines DataFrames along rows or columns.",
      "syntax": "pd.concat(objs, axis=0, ignore_index=False)",
      "description": "pandas.concat combines DataFrames along rows or columns. Use it for combining fact tables with lookup dimensions and cleaning and reshaping transaction/event tables. Key parameters: objs, axis, ignore_index.",
      "overview": "pandas.concat combines DataFrames along rows or columns. Use pandas.concat when combining fact tables with lookup dimensions. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: objs, axis, ignore_index. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: pandas.concat is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "pandas.concat official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.concat.html"
        }
      ],
      "parameters": [
        {
          "name": "objs",
          "type": "Any",
          "description": "Parameter `objs` configures how `pandas.concat` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "axis",
          "type": "int",
          "default": "0",
          "description": "Axis along which operation is applied.",
          "values": [
            "1",
            "5",
            "10"
          ],
          "pitfalls": [
            "Wrong axis silently changes calculation direction and interpretation."
          ]
        },
        {
          "name": "ignore_index",
          "type": "Any",
          "default": "False",
          "description": "Parameter `ignore_index` configures how `pandas.concat` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "pandas.concat for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = pd.concat([df.iloc[:2], df.iloc[2:]], ignore_index=True)\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run pandas.concat directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "pandas.concat additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = pd.concat([df.iloc[:2], df.iloc[2:]], ignore_index=True)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run pandas.concat again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-join",
      "slug": "pandas-join",
      "name": "DataFrame.join",
      "library": "pandas",
      "subtopic": "Join",
      "tags": [
        "join",
        "index"
      ],
      "level": "extended",
      "summary": "DataFrame.join merges DataFrames using index alignment or key columns.",
      "syntax": "df.join(other, on=None, how='left')",
      "description": "DataFrame.join merges DataFrames using index alignment or key columns. Use it for combining fact tables with lookup dimensions and combining transactional tables with customer/product reference data. Key parameters: other, on, how.",
      "overview": "DataFrame.join merges DataFrames using index alignment or key columns. Use DataFrame.join when combining fact tables with lookup dimensions. Typical business pattern: combining transactional tables with customer/product reference data. Key parameters: other, on, how. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.join is used for combining transactional tables with customer/product reference data.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.join official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.join.html"
        }
      ],
      "parameters": [
        {
          "name": "other",
          "type": "Any",
          "description": "Parameter `other` configures how `DataFrame.join` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "on",
          "type": "Any",
          "default": "None",
          "description": "Column/key selector for `DataFrame.join`. It controls grouping, joining, encoding, or styling based on the selected fields.",
          "values": [
            "single column name",
            "list of column names",
            "None (all applicable columns)"
          ],
          "pitfalls": [
            "Misspelled column names cause runtime errors.",
            "High-cardinality keys may increase memory and runtime."
          ]
        },
        {
          "name": "how",
          "type": "Any",
          "default": "'left'",
          "description": "Join/merge strategy for `DataFrame.join`. It determines which keys are retained from each side of the join.",
          "values": [
            "'left'",
            "'inner'",
            "'outer' / 'right'"
          ],
          "pitfalls": [
            "Use left joins when preserving the primary table is required.",
            "Check row counts before and after join to catch accidental duplication."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.join for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = df.join(other, how='left')\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run DataFrame.join directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "DataFrame.join additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.join(other, how='left')\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.join again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-drop-duplicates",
      "slug": "pandas-drop-duplicates",
      "name": "DataFrame.drop_duplicates",
      "library": "pandas",
      "subtopic": "Transformation",
      "tags": [
        "deduplication",
        "quality"
      ],
      "level": "extended",
      "summary": "DataFrame.drop_duplicates removes duplicate rows by selected columns.",
      "syntax": "df.drop_duplicates(subset=None, keep='first')",
      "description": "DataFrame.drop_duplicates removes duplicate rows by selected columns. Use it for cleaning and engineering columns in tabular datasets and cleaning and reshaping transaction/event tables. Key parameters: subset, keep.",
      "overview": "DataFrame.drop_duplicates removes duplicate rows by selected columns. Use DataFrame.drop_duplicates when cleaning and engineering columns in tabular datasets. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: subset, keep. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.drop_duplicates is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.drop_duplicates official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.drop_duplicates.html"
        }
      ],
      "parameters": [
        {
          "name": "subset",
          "type": "Any",
          "default": "None",
          "description": "Data input consumed by `DataFrame.drop_duplicates`. Changing this argument changes which rows/columns are processed and can alter output shape and statistics.",
          "values": [
            "DataFrame",
            "array-like object",
            "Series/list input"
          ],
          "pitfalls": [
            "Confirm required columns exist before running.",
            "Check dtypes so numeric operations/modeling behave as expected."
          ]
        },
        {
          "name": "keep",
          "type": "Any",
          "default": "'first'",
          "description": "Parameter `keep` configures how `DataFrame.drop_duplicates` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.drop_duplicates for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = df.drop_duplicates(subset=None, keep='first')\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run DataFrame.drop_duplicates directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "DataFrame.drop_duplicates additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.drop_duplicates(subset=None, keep='first')\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.drop_duplicates again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-isna",
      "slug": "pandas-isna",
      "name": "DataFrame.isna",
      "library": "pandas",
      "subtopic": "Missing Data",
      "tags": [
        "missing-data",
        "diagnostics"
      ],
      "level": "extended",
      "summary": "DataFrame.isna returns a boolean mask of missing values.",
      "syntax": "df.isna()",
      "description": "DataFrame.isna returns a boolean mask of missing values. Use it for auditing and remediating null-heavy source extracts and handling null-heavy CRM, finance, and operations exports. Most controls are optional keyword arguments.",
      "overview": "DataFrame.isna returns a boolean mask of missing values. Use DataFrame.isna when auditing and remediating null-heavy source extracts. Typical business pattern: handling null-heavy CRM, finance, and operations exports. Most controls are optional keyword arguments. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.isna is used for handling null-heavy CRM, finance, and operations exports.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.isna official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.isna.html"
        }
      ],
      "parameters": [
        {
          "name": "inputs",
          "type": "Any",
          "description": "Data input consumed by `DataFrame.isna`. Changing this argument changes which rows/columns are processed and can alter output shape and statistics.",
          "values": [
            "DataFrame",
            "array-like object",
            "Series/list input"
          ],
          "pitfalls": [
            "Confirm required columns exist before running.",
            "Check dtypes so numeric operations/modeling behave as expected."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.isna for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = df.isna()\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run DataFrame.isna directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "DataFrame.isna additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.isna()\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.isna again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-dropna",
      "slug": "pandas-dropna",
      "name": "DataFrame.dropna",
      "library": "pandas",
      "subtopic": "Missing Data",
      "tags": [
        "missing-data",
        "cleanup"
      ],
      "level": "extended",
      "summary": "DataFrame.dropna removes rows or columns containing missing values.",
      "syntax": "df.dropna(axis=0, how='any', subset=None)",
      "description": "DataFrame.dropna removes rows or columns containing missing values. Use it for auditing and remediating null-heavy source extracts and handling null-heavy CRM, finance, and operations exports. Key parameters: axis, how, subset.",
      "overview": "DataFrame.dropna removes rows or columns containing missing values. Use DataFrame.dropna when auditing and remediating null-heavy source extracts. Typical business pattern: handling null-heavy CRM, finance, and operations exports. Key parameters: axis, how, subset. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.dropna is used for handling null-heavy CRM, finance, and operations exports.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.dropna official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.dropna.html"
        }
      ],
      "parameters": [
        {
          "name": "axis",
          "type": "int",
          "default": "0",
          "description": "Axis along which operation is applied.",
          "values": [
            "1",
            "5",
            "10"
          ],
          "pitfalls": [
            "Wrong axis silently changes calculation direction and interpretation."
          ]
        },
        {
          "name": "how",
          "type": "Any",
          "default": "'any'",
          "description": "Join/merge strategy for `DataFrame.dropna`. It determines which keys are retained from each side of the join.",
          "values": [
            "'left'",
            "'inner'",
            "'outer' / 'right'"
          ],
          "pitfalls": [
            "Use left joins when preserving the primary table is required.",
            "Check row counts before and after join to catch accidental duplication."
          ]
        },
        {
          "name": "subset",
          "type": "Any",
          "default": "None",
          "description": "Data input consumed by `DataFrame.dropna`. Changing this argument changes which rows/columns are processed and can alter output shape and statistics.",
          "values": [
            "DataFrame",
            "array-like object",
            "Series/list input"
          ],
          "pitfalls": [
            "Confirm required columns exist before running.",
            "Check dtypes so numeric operations/modeling behave as expected."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.dropna for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = df.dropna(axis=0, how='any', subset=None)\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run DataFrame.dropna directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "DataFrame.dropna additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.dropna(axis=0, how='any', subset=None)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.dropna again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-replace",
      "slug": "pandas-replace",
      "name": "DataFrame.replace",
      "library": "pandas",
      "subtopic": "Transformation",
      "tags": [
        "cleaning",
        "values"
      ],
      "level": "extended",
      "summary": "DataFrame.replace substitutes values using scalar, list, dict, or regex rules.",
      "syntax": "df.replace(to_replace, value)",
      "description": "DataFrame.replace substitutes values using scalar, list, dict, or regex rules. Use it for cleaning and engineering columns in tabular datasets and cleaning and reshaping transaction/event tables. Key parameters: to_replace, value.",
      "overview": "DataFrame.replace substitutes values using scalar, list, dict, or regex rules. Use DataFrame.replace when cleaning and engineering columns in tabular datasets. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: to_replace, value. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.replace is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.replace official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.replace.html"
        }
      ],
      "parameters": [
        {
          "name": "to_replace",
          "type": "Any",
          "description": "Parameter `to_replace` configures how `DataFrame.replace` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "value",
          "type": "Any",
          "description": "Parameter `value` configures how `DataFrame.replace` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.replace for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = df.replace(-1, pd.NA)\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run DataFrame.replace directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "DataFrame.replace additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.replace(to_replace, records[0])\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.replace again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-cut",
      "slug": "pandas-cut",
      "name": "pandas.cut",
      "library": "pandas",
      "subtopic": "Transformation",
      "tags": [
        "binning",
        "segmentation"
      ],
      "level": "extended",
      "summary": "pandas.cut bins continuous values into interval categories.",
      "syntax": "pd.cut(x, bins, labels=None)",
      "description": "pandas.cut bins continuous values into interval categories. Use it for cleaning and engineering columns in tabular datasets and cleaning and reshaping transaction/event tables. Key parameters: x, bins, labels.",
      "overview": "pandas.cut bins continuous values into interval categories. Use pandas.cut when cleaning and engineering columns in tabular datasets. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: x, bins, labels. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: pandas.cut is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "pandas.cut official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.cut.html"
        }
      ],
      "parameters": [
        {
          "name": "x",
          "type": "Any",
          "description": "Data input consumed by `pandas.cut`. Changing this argument changes which rows/columns are processed and can alter output shape and statistics.",
          "values": [
            "DataFrame",
            "array-like object",
            "Series/list input"
          ],
          "pitfalls": [
            "Confirm required columns exist before running.",
            "Check dtypes so numeric operations/modeling behave as expected."
          ]
        },
        {
          "name": "bins",
          "type": "Any",
          "description": "Parameter `bins` configures how `pandas.cut` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "labels",
          "type": "str | list[str]",
          "default": "None",
          "description": "Target or label values used by `pandas.cut`. This controls what the model/metric learns or evaluates against.",
          "values": [
            "'auto'",
            "'left'",
            "'mean'"
          ],
          "pitfalls": [
            "Ensure `X` and `y` have matching row counts.",
            "Avoid leakage by excluding post-outcome fields from features."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "pandas.cut for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = pd.cut(df['value'], bins=[0, 15, 30, 50], labels=['low','mid','high'])\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run pandas.cut directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "pandas.cut additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = pd.cut(df['records[0]'], bins=[0,15,30,50], labels=['low','mid','high'])\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run pandas.cut again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-qcut",
      "slug": "pandas-qcut",
      "name": "pandas.qcut",
      "library": "pandas",
      "subtopic": "Transformation",
      "tags": [
        "quantiles",
        "segmentation"
      ],
      "level": "extended",
      "summary": "pandas.qcut bins values into quantile-based buckets.",
      "syntax": "pd.qcut(x, q, labels=None)",
      "description": "pandas.qcut bins values into quantile-based buckets. Use it for cleaning and engineering columns in tabular datasets and cleaning and reshaping transaction/event tables. Key parameters: x, q, labels.",
      "overview": "pandas.qcut bins values into quantile-based buckets. Use pandas.qcut when cleaning and engineering columns in tabular datasets. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameters: x, q, labels. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: pandas.qcut is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "pandas.qcut official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.qcut.html"
        }
      ],
      "parameters": [
        {
          "name": "x",
          "type": "Any",
          "description": "Data input consumed by `pandas.qcut`. Changing this argument changes which rows/columns are processed and can alter output shape and statistics.",
          "values": [
            "DataFrame",
            "array-like object",
            "Series/list input"
          ],
          "pitfalls": [
            "Confirm required columns exist before running.",
            "Check dtypes so numeric operations/modeling behave as expected."
          ]
        },
        {
          "name": "q",
          "type": "Any",
          "description": "Parameter `q` configures how `pandas.qcut` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "labels",
          "type": "str | list[str]",
          "default": "None",
          "description": "Target or label values used by `pandas.qcut`. This controls what the model/metric learns or evaluates against.",
          "values": [
            "'auto'",
            "'left'",
            "'mean'"
          ],
          "pitfalls": [
            "Ensure `X` and `y` have matching row counts.",
            "Avoid leakage by excluding post-outcome fields from features."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "pandas.qcut for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = pd.qcut(df['value'], q=2, labels=['low','high'])\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run pandas.qcut directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "pandas.qcut additional business case",
          "scenario": "Second realistic usage variant where the same command is applied to a different data slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = pd.qcut(df['records[0]'], q=2, labels=['low','high'])\nprint(type(result).__name__)",
          "expectedOutput": "A second executable output pattern that still uses the documented command.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run pandas.qcut exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-rolling",
      "slug": "pandas-rolling",
      "name": "DataFrame.rolling",
      "library": "pandas",
      "subtopic": "Time Series",
      "tags": [
        "window",
        "time-series"
      ],
      "level": "extended",
      "summary": "DataFrame.rolling creates moving window views for time-series statistics.",
      "syntax": "df.rolling(window, min_periods=None)",
      "description": "DataFrame.rolling creates moving window views for time-series statistics. Use it for constructing lag, rolling, and resampled temporal features and tracking daily/weekly performance with lag and rolling signals. Key parameters: window, min_periods.",
      "overview": "DataFrame.rolling creates moving window views for time-series statistics. Use DataFrame.rolling when constructing lag, rolling, and resampled temporal features. Typical business pattern: tracking daily/weekly performance with lag and rolling signals. Key parameters: window, min_periods. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.rolling is used for tracking daily/weekly performance with lag and rolling signals.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.rolling official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.rolling.html"
        }
      ],
      "parameters": [
        {
          "name": "window",
          "type": "Any",
          "description": "Parameter `window` configures how `DataFrame.rolling` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "min_periods",
          "type": "int",
          "default": "None",
          "description": "Parameter `min_periods` configures how `DataFrame.rolling` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "1",
            "5",
            "10"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.rolling primary workflow",
          "scenario": "DataFrame.rolling used in a realistic business workflow with active command execution.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df[['records[0]']].rolling(window=2)\nprint(type(result).__name__)",
          "expectedOutput": "Executable output object/value that confirms the command ran on the sample dataset.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run DataFrame.rolling exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        },
        {
          "title": "DataFrame.rolling additional business case",
          "scenario": "Second realistic usage variant where the same command is applied to a different data slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df[['records[0]']].rolling(window=2)\nprint(type(result).__name__)",
          "expectedOutput": "A second executable output pattern that still uses the documented command.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run DataFrame.rolling exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-shift",
      "slug": "pandas-shift",
      "name": "DataFrame.shift",
      "library": "pandas",
      "subtopic": "Time Series",
      "tags": [
        "lag",
        "features"
      ],
      "level": "extended",
      "summary": "DataFrame.shift offsets rows for lag/lead features.",
      "syntax": "df.shift(periods=1)",
      "description": "DataFrame.shift offsets rows for lag/lead features. Use it for constructing lag, rolling, and resampled temporal features and cleaning and reshaping transaction/event tables. Key parameter: periods.",
      "overview": "DataFrame.shift offsets rows for lag/lead features. Use DataFrame.shift when constructing lag, rolling, and resampled temporal features. Typical business pattern: cleaning and reshaping transaction/event tables. Key parameter: periods. Returns DataFrame | Series | scalar with shape (rows, columns) or scalar.",
      "applications": [
        "Primary application: DataFrame.shift is used for cleaning and reshaping transaction/event tables.",
        "Workflow application: treat this command as a tabular transformation step in ETL/EDA/model-prep workflows.",
        "Delivery application: document chosen parameters and output checks so teammates can reproduce results."
      ],
      "docs": [
        {
          "label": "DataFrame.shift official docs",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.shift.html"
        }
      ],
      "parameters": [
        {
          "name": "periods",
          "type": "Any",
          "default": "1",
          "description": "Parameter `periods` configures how `DataFrame.shift` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "DataFrame | Series | scalar",
        "shape": "(rows, columns) or scalar",
        "description": "Tabular transformation or summary result."
      },
      "examples": [
        {
          "title": "DataFrame.shift for segmented revenue reporting",
          "scenario": "Revenue analytics workflow using segmented business records and repeatable table transformations.",
          "code": "import pandas as pd\n\ndf = pd.DataFrame({\n    'segment': ['A', 'A', 'B', 'C'],\n    'value': [10, 20, 30, 40],\n    'date': pd.date_range('2026-01-01', periods=4)\n})\nother = pd.DataFrame({'tier': ['gold', 'gold', 'silver', 'bronze']}, index=df.index)\n\nresult = df.shift(periods=1)\n\nif hasattr(result, 'shape'):\n    print(type(result).__name__, result.shape)\nelse:\n    print(type(result).__name__, result)",
          "expectedOutput": "Printed output shows command result type and shape/value, ready to adapt to your real table.",
          "walkthrough": [
            "Create a small DataFrame that mirrors your business columns (segment, value, date).",
            "Run DataFrame.shift directly (not commented) and store output in `result`.",
            "Check output type/shape, then swap in your project DataFrame and keep the same pattern."
          ]
        },
        {
          "title": "DataFrame.shift additional business case",
          "scenario": "Second realistic usage variant for adapting the command to another business dataset slice.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30]})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df.shift(periods=1)\nprint(type(result).__name__)",
          "expectedOutput": "Output confirms executable pattern for a second business use case.",
          "walkthrough": [
            "Create a second toy business slice (different segment/time window).",
            "Run DataFrame.shift again on this alternate slice to validate transferability.",
            "Move the same pattern into your production notebook/script with assertions."
          ]
        }
      ],
      "notes": [
        "Start with toy data and explicit parameters, then scale to full datasets.",
        "Always verify version-specific defaults in official docs before production deployment."
      ],
      "related": []
    },
    {
      "id": "pandas-dt-year",
      "name": "Series.dt.year",
      "subtopic": "Time Series",
      "tags": [
        "datetime",
        "dt-accessor",
        "eda"
      ],
      "level": "core",
      "summary": "Extract calendar year for cohort and annual trend analysis.",
      "syntax": "df['order_year']=df['order_date'].dt.year",
      "description": "Fast year extraction once a column is parsed to datetime dtype.",
      "overview": "Extract calendar year for cohort and annual trend analysis. Use dt.year to create stable annual grouping keys for retention, growth, and seasonality analysis. Typical pattern: parse order_date with to_datetime, derive order_year, then groupby annual KPIs. Returns integer series aligned to row count.",
      "applications": [
        "Primary application: build annual cohorts and yearly KPI rollups.",
        "Workflow application: derive year once and reuse across EDA and modeling.",
        "Delivery application: keep explicit year fields in outputs for reproducibility."
      ],
      "docs": [
        {
          "label": "Series.dt.year",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.Series.dt.year.html"
        }
      ],
      "parameters": [
        {
          "name": "datetime series",
          "type": "pandas.Series[datetime64]",
          "description": "Datetime column for year extraction.",
          "values": [
            "df['order_date']",
            "df['signup_date']"
          ],
          "pitfalls": [
            "Object dtype must be parsed first using pandas.to_datetime."
          ]
        }
      ],
      "returns": {
        "type": "pandas.Series[int]",
        "shape": "same rows as input",
        "description": "Year value per row."
      },
      "examples": [
        {
          "title": "Series.dt.year primary workflow",
          "scenario": "Series.dt.year used in a realistic business workflow with active command execution.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df['order_year']=df['order_date'].dt.year\nprint(type(result).__name__)",
          "expectedOutput": "Executable output object/value that confirms the command ran on the sample dataset.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run Series.dt.year exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        }
      ],
      "notes": [
        "Use dt.isocalendar().year if ISO week-year is required."
      ],
      "related": [
        "pandas-to-datetime",
        "pandas-dt-components",
        "pandas-groupby-basic"
      ],
      "slug": "pandas-dt-year",
      "library": "pandas"
    },
    {
      "id": "pandas-dt-components",
      "name": "Series.dt.month / day_name / quarter",
      "subtopic": "Time Series",
      "tags": [
        "datetime",
        "seasonality",
        "feature-engineering"
      ],
      "level": "extended",
      "summary": "Create month/weekday/quarter features for seasonal EDA.",
      "syntax": "df=df.assign(month=df['order_date'].dt.month,weekday=df['order_date'].dt.day_name(),quarter=df['order_date'].dt.quarter)",
      "description": "Derive calendar components to explain weekly and quarterly patterns.",
      "overview": "Create month/weekday/quarter features for seasonal EDA. Use dt components when exploring cyclic behavior, weekday effects, and quarter-level shifts. Typical pattern: add month and weekday columns, then run groupby summaries to compare demand/conversion across calendar slices. Returns added feature columns with same row count.",
      "applications": [
        "Primary application: identify seasonal patterns hidden in raw timestamps.",
        "Workflow application: engineer calendar features before model training.",
        "Delivery application: report weekday/month effects in stakeholder-friendly terms."
      ],
      "docs": [
        {
          "label": "Datetime properties",
          "url": "https://pandas.pydata.org/docs/reference/series.html#datetimelike-properties"
        }
      ],
      "parameters": [
        {
          "name": "datetime series",
          "type": "pandas.Series[datetime64]",
          "description": "Source datetime column.",
          "values": [
            "df['order_date']"
          ],
          "pitfalls": [
            "NaT rows propagate missing calendar components."
          ]
        },
        {
          "name": "month",
          "type": "Any",
          "description": "Parameter `month` configures how `Series.dt.month / day_name / quarter` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "df['order_date'].dt.month"
        },
        {
          "name": "weekday",
          "type": "int",
          "description": "Parameter `weekday` configures how `Series.dt.month / day_name / quarter` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "1",
            "5",
            "10"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "df['order_date'].dt.day_name()"
        },
        {
          "name": "quarter",
          "type": "Any",
          "description": "Parameter `quarter` configures how `Series.dt.month / day_name / quarter` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "df['order_date'].dt.quarter"
        }
      ],
      "returns": {
        "type": "pandas.DataFrame (new columns)",
        "shape": "same rows, +calendar columns",
        "description": "Calendar-derived features for EDA/modeling."
      },
      "examples": [
        {
          "title": "Series.dt.month / day_name / quarter primary workflow",
          "scenario": "Series.dt.month / day_name / quarter used in a realistic business workflow with active command execution.",
          "code": "import pandas as pd\ndf = pd.DataFrame({'segment':['A','B','A'], 'value':[10,20,30], 'date': pd.date_range('2026-01-01', periods=3)})\nother = pd.DataFrame({'tier':['gold','silver','gold']}, index=df.index)\nresult = df=df.assign(month=df['order_date'].dt.month,weekday=df['order_date'].dt.day_name(),quarter=df['order_date'].dt.quarter)\nprint(type(result).__name__)",
          "expectedOutput": "Executable output object/value that confirms the command ran on the sample dataset.",
          "walkthrough": [
            "Create a toy dataset that mirrors your project columns and value types.",
            "Run Series.dt.month / day_name / quarter exactly as shown and inspect the output type/shape.",
            "Transfer the same command pattern into your production notebook or pipeline."
          ]
        }
      ],
      "notes": [
        "Convert weekday to ordered categorical for Monday-Sunday chart ordering."
      ],
      "related": [
        "pandas-to-datetime",
        "pandas-dt-year",
        "pandas-resample"
      ],
      "slug": "pandas-dt-components",
      "library": "pandas"
    },
    {
      "id": "pandas-datetime-filter",
      "name": "Datetime Filtering (.between / .dt)",
      "subtopic": "Time Series",
      "tags": [
        "datetime",
        "filtering",
        "eda"
      ],
      "level": "core",
      "summary": "Filter by date range and calendar conditions (weekday/month/quarter).",
      "syntax": "q1_weekdays=df.loc[df['order_date'].between('2026-01-01','2026-03-31') & (df['order_date'].dt.dayofweek < 5)]",
      "description": "Scopes analysis windows precisely for fair comparisons and cleaner EDA.",
      "overview": "Filter by date range and calendar conditions (weekday/month/quarter). Use datetime filtering for pre/post analyses, quarter slices, or business-day-only KPI comparisons. Typical pattern: combine between(start,end) with dt.dayofweek/month constraints to isolate a valid analysis slice. Returns filtered DataFrame.",
      "applications": [
        "Primary application: isolate a valid analysis window before computing KPIs.",
        "Workflow application: centralize date filters near top of notebook for consistency.",
        "Delivery application: report exact date boundaries with every metric output."
      ],
      "docs": [
        {
          "label": "Indexing guide",
          "url": "https://pandas.pydata.org/docs/user_guide/indexing.html"
        }
      ],
      "parameters": [
        {
          "name": "start/end bounds",
          "type": "str | Timestamp",
          "description": "Date range boundaries.",
          "values": [
            "'2026-01-01'",
            "'2026-03-31'"
          ],
          "pitfalls": [
            "Timezone mismatch can shift boundaries unexpectedly."
          ]
        },
        {
          "name": "dt condition",
          "type": "boolean mask",
          "description": "Calendar rule on datetime accessor.",
          "values": [
            "df['order_date'].dt.dayofweek < 5",
            "df['order_date'].dt.month.isin([11,12])"
          ],
          "pitfalls": [
            "Missing parentheses in boolean logic changes filter meaning."
          ]
        },
        {
          "name": "'2026-01-01'",
          "type": "Any",
          "description": "Parameter `'2026-01-01'` configures how `Datetime Filtering (.between / .dt)` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "'2026-03-31')&(df['order_date'].dt.dayofweek<5",
          "type": "int",
          "description": "Parameter `'2026-03-31')&(df['order_date'].dt.dayofweek<5` configures how `Datetime Filtering (.between / .dt)` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "1",
            "5",
            "10"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "shape": "(filtered_rows, columns)",
        "description": "Windowed subset for scoped analysis."
      },
      "examples": [
        {
          "title": "Q1 business-day slice",
          "scenario": "Finance compares Q1 weekday performance only.",
          "code": "import pandas as pd\n\ndf=pd.DataFrame({'order_date':pd.to_datetime(['2026-01-02','2026-01-03','2026-04-01']),'revenue':[120,220,140]})\nmask=df['order_date'].between('2026-01-01','2026-03-31') & (df['order_date'].dt.dayofweek < 5)\nprint(df.loc[mask])",
          "expectedOutput": "Only Q1 weekday rows remain.",
          "walkthrough": [
            "Define range.",
            "Apply dt weekday mask.",
            "Use filtered frame for downstream EDA."
          ]
        }
      ],
      "notes": [
        "If the date is index-based, date slicing with .loc can be simpler."
      ],
      "related": [
        "pandas-to-datetime",
        "pandas-query",
        "pandas-dt-components"
      ],
      "slug": "pandas-datetime-filter",
      "library": "pandas"
    },
    {
      "id": "pandas-groupby-basic",
      "name": "DataFrame.groupby + size/count",
      "subtopic": "Aggregation",
      "tags": [
        "groupby",
        "counts",
        "eda"
      ],
      "level": "core",
      "summary": "Start with segment-level row counts and non-null counts.",
      "syntax": "df.groupby('segment',as_index=False).size()",
      "description": "First-pass group audit for sample balance and missingness by segment.",
      "overview": "Start with segment-level row counts and non-null counts. Use size vs count as an EDA checkpoint before complex aggregations/modeling. Typical pattern: check whether groups are under-sampled and whether key metrics are missing unevenly across segments. Returns compact group diagnostics.",
      "applications": [
        "Primary application: validate sample size adequacy by segment.",
        "Workflow application: compare size vs count to localize missing-data risk.",
        "Delivery application: include count diagnostics in EDA summaries for context."
      ],
      "docs": [
        {
          "label": "Groupby guide",
          "url": "https://pandas.pydata.org/docs/user_guide/groupby.html"
        }
      ],
      "parameters": [
        {
          "name": "by",
          "type": "str | list[str]",
          "description": "Column/key selector for `DataFrame.groupby + size/count`. It controls grouping, joining, encoding, or styling based on the selected fields.",
          "values": [
            "'segment'",
            "['region','plan']"
          ],
          "pitfalls": [
            "Too-granular keys create noisy sparse groups."
          ]
        },
        {
          "name": "count target",
          "type": "str",
          "description": "Column for non-null count comparison.",
          "values": [
            "'revenue'"
          ],
          "pitfalls": [
            "count excludes nulls; size includes all rows."
          ]
        },
        {
          "name": "'segment'",
          "type": "Any",
          "description": "Parameter `'segment'` configures how `DataFrame.groupby + size/count` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "as_index",
          "type": "Any",
          "description": "Parameter `as_index` configures how `DataFrame.groupby + size/count` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "False).size("
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "shape": "(groups, count columns)",
        "description": "Group-level count diagnostics."
      },
      "examples": [
        {
          "title": "Size vs count check",
          "scenario": "Analyst checks missingness concentration before KPI reporting.",
          "code": "import pandas as pd\n\ndf=pd.DataFrame({'segment':['A','A','B','B','C'],'revenue':[100,None,80,95,None]})\nsize_tbl=df.groupby('segment',as_index=False).size()\ncount_tbl=df.groupby('segment',as_index=False).agg(revenue_non_null=('revenue','count'))\nprint(size_tbl.merge(count_tbl,on='segment'))",
          "expectedOutput": "Per-segment total rows and non-null revenue counts.",
          "walkthrough": [
            "Run size.",
            "Run count on key measure.",
            "Compare and flag weak segments."
          ]
        }
      ],
      "notes": [
        "Use this check in every new EDA pipeline before deep analysis."
      ],
      "related": [
        "pandas-groupby-agg",
        "pandas-isna",
        "pandas-nunique"
      ],
      "slug": "pandas-groupby-basic",
      "library": "pandas"
    },
    {
      "id": "pandas-groupby-named-agg",
      "name": "GroupBy Named Aggregations",
      "subtopic": "Aggregation",
      "tags": [
        "groupby",
        "named-aggregation",
        "kpi"
      ],
      "level": "extended",
      "summary": "Define multi-metric group summaries with explicit output names.",
      "syntax": "df.groupby('channel',as_index=False).agg(total_revenue=('revenue','sum'),avg_order=('revenue','mean'),customers=('customer_id','nunique'))",
      "description": "Named metrics prevent ambiguous columns and make KPI outputs easy to reuse.",
      "overview": "Define multi-metric group summaries with explicit output names. Use this pattern for business scorecards where metric clarity matters and outputs are reused in dashboards/models. Typical pattern: one grouped table combining totals, means, counts, and distinct counts per segment. Returns flat-column DataFrame ready for publishing.",
      "applications": [
        "Primary application: generate report-ready KPI tables in one pass.",
        "Workflow application: keep metric naming stable across notebooks and production jobs.",
        "Delivery application: avoid post-processing renames by naming metrics in agg directly."
      ],
      "docs": [
        {
          "label": "Named aggregation",
          "url": "https://pandas.pydata.org/docs/user_guide/groupby.html#named-aggregation"
        }
      ],
      "parameters": [
        {
          "name": "group keys",
          "type": "str | list[str]",
          "description": "Segmentation dimensions.",
          "values": [
            "'channel'",
            "['month','region']"
          ],
          "pitfalls": [
            "Changing keys changes interpretation and comparability."
          ]
        },
        {
          "name": "named metrics",
          "type": "tuple mapping",
          "description": "Metric definitions with output names.",
          "values": [
            "total_revenue=('revenue','sum')",
            "customers=('customer_id','nunique')"
          ],
          "pitfalls": [
            "Inconsistent naming across analyses creates confusion."
          ]
        },
        {
          "name": "'channel'",
          "type": "Any",
          "description": "Parameter `'channel'` configures how `GroupBy Named Aggregations` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "as_index",
          "type": "Any",
          "description": "Parameter `as_index` configures how `GroupBy Named Aggregations` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "False).agg(total_revenue=('revenue'"
        },
        {
          "name": "'sum')",
          "type": "Any",
          "description": "Parameter `'sum')` configures how `GroupBy Named Aggregations` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "avg_order",
          "type": "Any",
          "description": "Parameter `avg_order` configures how `GroupBy Named Aggregations` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ],
          "default": "('revenue'"
        },
        {
          "name": "'mean')",
          "type": "Any",
          "description": "Parameter `'mean')` configures how `GroupBy Named Aggregations` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        },
        {
          "name": "customers",
          "type": "Any",
          "description": "Data input consumed by `GroupBy Named Aggregations`. Changing this argument changes which rows/columns are processed and can alter output shape and statistics.",
          "values": [
            "DataFrame",
            "array-like object",
            "Series/list input"
          ],
          "pitfalls": [
            "Confirm required columns exist before running.",
            "Check dtypes so numeric operations/modeling behave as expected."
          ],
          "default": "('customer_id'"
        },
        {
          "name": "'nunique')",
          "type": "Any",
          "description": "Parameter `'nunique')` configures how `GroupBy Named Aggregations` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "shape": "(groups, metrics + keys)",
        "description": "Flat KPI summary with explicit metric names."
      },
      "examples": [
        {
          "title": "Channel scorecard",
          "scenario": "Growth team needs clear weekly metrics by acquisition channel.",
          "code": "import pandas as pd\n\ndf=pd.DataFrame({'channel':['Email','Paid','Email','Organic'],'customer_id':[1,2,1,3],'revenue':[80,140,95,120]})\nout=df.groupby('channel',as_index=False).agg(total_revenue=('revenue','sum'),avg_order=('revenue','mean'),customers=('customer_id','nunique'))\nprint(out)",
          "expectedOutput": "KPI table by channel with readable metric names.",
          "walkthrough": [
            "Group by business segment.",
            "Use named metrics.",
            "Publish directly without column cleanup."
          ]
        }
      ],
      "notes": [
        "Prefer named aggregations to avoid MultiIndex outputs."
      ],
      "related": [
        "pandas-groupby-agg",
        "pandas-groupby-share-total",
        "pandas-pivot-table"
      ],
      "slug": "pandas-groupby-named-agg",
      "library": "pandas"
    },
    {
      "id": "pandas-groupby-share-total",
      "name": "GroupBy.transform (Share of Group Total)",
      "subtopic": "Aggregation",
      "tags": [
        "groupby",
        "transform",
        "normalization"
      ],
      "level": "extended",
      "summary": "Compute each row as a share of its group total.",
      "syntax": "df['share_in_region']=df['revenue']/df.groupby('region')['revenue'].transform('sum')",
      "description": "Creates normalized within-group features for fair cross-segment comparisons.",
      "overview": "Compute each row as a share of its group total. Use this transform pattern when absolute values are hard to compare across groups and relative contribution matters. Typical pattern: product share within region, campaign share within channel, or customer spend share within cohort. Returns row-level proportion feature aligned to original data.",
      "applications": [
        "Primary application: expose concentration/mix effects inside each segment.",
        "Workflow application: pair share metrics with rank to identify top contributors.",
        "Delivery application: present both absolute and share metrics for balanced decisions."
      ],
      "docs": [
        {
          "label": "SeriesGroupBy.transform",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.core.groupby.SeriesGroupBy.transform.html"
        }
      ],
      "parameters": [
        {
          "name": "group key",
          "type": "str | list[str]",
          "description": "Segmentation key for denominator totals.",
          "values": [
            "'region'",
            "['region','month']"
          ],
          "pitfalls": [
            "Wrong group key yields misleading share values."
          ]
        },
        {
          "name": "metric",
          "type": "numeric Series",
          "description": "Value normalized by group sum.",
          "values": [
            "df['revenue']",
            "df['orders']"
          ],
          "pitfalls": [
            "Handle zero denominators before dividing."
          ]
        },
        {
          "name": "'region')['revenue'].transform('sum'",
          "type": "Any",
          "description": "Parameter `'region')['revenue'].transform('sum'` configures how `GroupBy.transform (Share of Group Total)` runs. Changing it affects output shape, behavior, or performance for your dataset.",
          "values": [
            "library default",
            "dataset-specific value"
          ],
          "pitfalls": [
            "Use values compatible with your data types and task objective.",
            "Record non-default choices so teammates can reproduce results."
          ]
        }
      ],
      "returns": {
        "type": "pandas.Series[float]",
        "shape": "same rows as input",
        "description": "Within-group share feature."
      },
      "examples": [
        {
          "title": "Regional revenue mix",
          "scenario": "Merchandising wants product contribution by region.",
          "code": "import pandas as pd\n\ndf=pd.DataFrame({'region':['W','W','E','E'],'product':['A','B','A','C'],'revenue':[120,80,90,210]})\ndf['share_in_region']=df['revenue']/df.groupby('region')['revenue'].transform('sum')\nprint(df)",
          "expectedOutput": "Rows include share_in_region that sums to 1 within each region.",
          "walkthrough": [
            "Choose business segment key.",
            "Compute group sums with transform.",
            "Derive and validate share feature."
          ]
        }
      ],
      "notes": [
        "For skewed groups, inspect both share and absolute values together."
      ],
      "related": [
        "pandas-groupby-transform",
        "pandas-groupby-named-agg",
        "pandas-sort-values"
      ],
      "slug": "pandas-groupby-share-total",
      "library": "pandas"
    },
    {
      "id": "pandas-corr",
      "name": "DataFrame.corr",
      "subtopic": "EDA",
      "tags": [
        "eda",
        "correlation",
        "numeric"
      ],
      "level": "extended",
      "summary": "Screen linear relationships among numeric variables quickly.",
      "syntax": "df[['spend','clicks','revenue']].corr(numeric_only=True)",
      "description": "Useful early EDA step for feature selection and multicollinearity awareness.",
      "overview": "Screen linear relationships among numeric variables quickly. Use DataFrame.corr during initial EDA to identify highly related predictors and confirm directional intuition before modeling. Typical pattern: analyze spend, traffic, conversion, and revenue metrics together to prioritize features and transformations. Returns square correlation matrix.",
      "applications": [
        "Primary application: identify redundant or strongly related numeric features.",
        "Workflow application: use correlation findings to guide feature pruning and interactions.",
        "Delivery application: report correlation with business context, not as causal proof."
      ],
      "docs": [
        {
          "label": "DataFrame.corr",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.corr.html"
        }
      ],
      "parameters": [
        {
          "name": "method",
          "type": "str",
          "default": "'pearson'",
          "description": "Correlation method.",
          "values": [
            "'pearson'",
            "'spearman'",
            "'kendall'"
          ],
          "pitfalls": [
            "Pearson is sensitive to outliers and only linear."
          ]
        },
        {
          "name": "numeric_only",
          "type": "bool",
          "default": "False",
          "description": "Use only numeric columns.",
          "values": [
            "True",
            "False"
          ],
          "pitfalls": [
            "Mixed dtypes may need explicit column selection."
          ]
        }
      ],
      "returns": {
        "type": "pandas.DataFrame",
        "shape": "(numeric_cols, numeric_cols)",
        "description": "Pairwise correlation matrix."
      },
      "examples": [
        {
          "title": "Quick driver screen",
          "scenario": "Analyst prepares baseline regression and wants a first correlation pass.",
          "code": "import pandas as pd\n\ndf=pd.DataFrame({'spend':[100,120,150],'clicks':[400,450,560],'revenue':[1200,1300,1660]})\nprint(df.corr(numeric_only=True))",
          "expectedOutput": "Numeric correlation matrix for selected columns.",
          "walkthrough": [
            "Select core numeric features.",
            "Compute corr matrix.",
            "Use high-correlation pairs to guide next checks."
          ]
        }
      ],
      "notes": [
        "Correlation does not imply causation; verify with experiment/time controls."
      ],
      "related": [
        "pandas-describe",
        "pandas-groupby-agg",
        "pandas-nunique"
      ],
      "slug": "pandas-corr",
      "library": "pandas"
    },
    {
      "id": "pandas-nunique",
      "name": "DataFrame.nunique",
      "subtopic": "EDA",
      "tags": [
        "eda",
        "cardinality",
        "data-quality"
      ],
      "level": "core",
      "summary": "Profile distinct counts per column for key/cardinality sanity checks.",
      "syntax": "df.nunique(dropna=False).sort_values(ascending=False)",
      "description": "Fast onboarding command for understanding feature granularity and key uniqueness.",
      "overview": "Profile distinct counts per column for key/cardinality sanity checks. Use nunique at the start of EDA to spot near-constant features, high-cardinality IDs, and duplicate-key risk. Typical pattern: verify whether order_id/customer_id uniqueness matches expectations and whether categorical fields are manageable. Returns distinct-count series.",
      "applications": [
        "Primary application: validate key uniqueness and feature cardinality.",
        "Workflow application: combine with null-rate and dtype audit in dataset onboarding.",
        "Delivery application: include cardinality summary in EDA handoff documents."
      ],
      "docs": [
        {
          "label": "DataFrame.nunique",
          "url": "https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.nunique.html"
        }
      ],
      "parameters": [
        {
          "name": "dropna",
          "type": "bool",
          "default": "True",
          "description": "Include/exclude NaN in counts.",
          "values": [
            "True",
            "False"
          ],
          "pitfalls": [
            "Inconsistent dropna setting can skew comparisons."
          ]
        }
      ],
      "returns": {
        "type": "pandas.Series",
        "shape": "(number_of_columns,)",
        "description": "Distinct count per column."
      },
      "examples": [
        {
          "title": "Cardinality audit",
          "scenario": "New dataset needs key uniqueness and categorical cardinality check.",
          "code": "import pandas as pd\n\ndf=pd.DataFrame({'customer_id':[1,2,2,4],'region':['W','E','E','W'],'plan':['basic','pro','pro',None]})\nprint(df.nunique(dropna=False).sort_values(ascending=False))",
          "expectedOutput": "Series of distinct counts by column.",
          "walkthrough": [
            "Run nunique early.",
            "Inspect keys for duplicates.",
            "Use counts to decide feature treatment."
          ]
        }
      ],
      "notes": [
        "Pair with duplicated/drop_duplicates for direct duplicate-row diagnostics."
      ],
      "related": [
        "pandas-info",
        "pandas-drop-duplicates",
        "pandas-groupby-basic"
      ],
      "slug": "pandas-nunique",
      "library": "pandas"
    }
  ],
  "overview": "Pandas is the core tabular toolkit for import, cleaning, EDA, datetime analysis, groupby summarization, and model-ready feature preparation.",
  "applications": [
    "Run robust first-pass EDA with schema checks, missingness audits, and cardinality profiling.",
    "Engineer datetime features (dt.year/month/weekday) and scoped date filters for reliable temporal analysis.",
    "Build repeatable groupby KPI tables and row-level share features for reporting and modeling."
  ]
}
